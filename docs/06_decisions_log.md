# 06b. 決定ログ(Decisions Log)

> セッション間で**決定だけ**を時系列に積む台帳。
> 状況・作業内容の申し送りは `docs/06_handoff.md`。**こちらは「何を決めたか」だけ。**
>
> ⚠️ **2026-08-23 新設。** v0.2 改訂プロンプトは「前回の引き継ぎ(docs/06_decisions_log.md)」を
> 参照していたが、**このリポジトリにそのファイルは存在しなかった**(あるのは `06_handoff.md`)。
> 別プロジェクト/別クローンの記憶と思われる。過去の決定は `06_handoff.md` に散在しているため、
> **本ログは 2026-08-23 の v0.2 改訂から開始する。** 遡及入力はしていない。

---

## 2026-08-23 — v0.2 全面改訂

オーナーのプロンプトによるブランド再定義。**これ以前の決定と矛盾する場合は v0.2 が勝つ。**

### ブランド

| # | 決定 | 上書きした旧決定 | 記録先 |
|---|---|---|---|
| D-001 | ブランド名は **MORU**(正式名称 **MORU LIVING**)。**「MORI」は誤記** | — | docs/00 §0-1 |
| D-002 | 再定義: **Pet-first, Interior-always.** ペット用品専門店にならない | 「猫用家具から始まるライフスタイルブランド」 | docs/00 §0-2 |
| D-003 | Concept / Mission / Vision / Values(5つ)を確定 | — | docs/00 §0-3, §0-4 |
| D-004 | 解決する問題は3層(Functional / Interior / **Emotional**)。Emotional の一文がコピーの源泉 | — | docs/00 §0-5 |
| D-005 | ペルソナ: 25〜40歳・都市部・猫と暮らすデザイン感度の高い層。安さ最優先ではない | — | docs/00 §0-6 |
| D-006 | 商品システム4階層(Hero / Core / Brand Builders / AOV・Cross-sell)。**階層が決まらない商品は入れない** | — | docs/00 §0-7 |
| D-007 | エコシステム(Problem発見→Hero→広告→サイト→回遊→Cross-sell→UGC→次のProblem)を定義 | — | docs/00 §0-8 |

### デザイン

| # | 決定 | 上書きした旧決定 | 記録先 |
|---|---|---|---|
| D-008 | **カラーパレット9色に刷新。** 署名は「MORUブルー `#1E5B8F` × テラコッタ `#D65832`」 | 旧4+4色(`#D45F35`/`#F7F2EA`/`#626747`/`#292723` 系) | docs/01 §2 |
| D-009 | 写真の4原則: **明るい自然光 / 抜け感 / 清潔感 / 青アクセント** | 「陽当たりのいい部屋の雑誌」(硬さ6/10) | docs/01 §3 |
| D-010 | Playful は**差し色1〜2色 + 遊び心のある形**で出す。原色の乱舞にしない | — | docs/01 §3 |
| D-011 | **⚠️ 明示的な方針転換: SALE系を解禁。** 番号付きベストセラー・NEWバッジ・セールバナー・組み合わせ割は**使用可** | v0.1「バッジ乱用・ランキング・NEW を避ける」 | docs/01 §4 |
| D-012 | ただし**偽の希少性は引き続き禁止**(偽カウントダウン・偽在庫・実績なき二重価格)。判定基準は「事実か否か」 | — | docs/01 §4 |
| D-013 | **サムネイルは白抜き禁止。明るい生活シーンの中の商品が正** | v0.1「Warm Ivory系の単色背景で統一」 | docs/01 §6 / シート03 |
| D-014 | ビジュアルの正を `docs/brand/reference-sheets/` シート01〜05 に移す。旧 `home_v2_final.png` は履歴(**削除しない**) | — | シート README |
| D-015 | 維持: ロゴ運用(差し替え可能)/ 猫耳・肉球UI禁止 / 未確認の品質表現禁止 | — | docs/01 §5, §7 |

### 構造

| # | 決定 | 上書きした旧決定 | 記録先 |
|---|---|---|---|
| D-016 | **ナビカテゴリ6分類確定(案A)**: ペット / チェア・スツール / テーブル / 照明 / ファブリック / 雑貨 | v2「ペット / チェア / テーブル / 照明 / 雑貨」(暫定) | docs/09 §1 |
| D-017 | デスクトップヘッダーはトップレベル5項目 + 「インテリア ▾」ドロップダウン。**雑貨はトップレベルに置かない** | v2 の左ナビ5項目 | docs/09 §2 |
| D-018 | モバイル: ペットは直リンク / インテリアはアコーディオン / 下段に信頼情報3項目 / カート常時表示 | — | docs/09 §3 |
| D-019 | コレクションは**スマートコレクション(タグ条件)**で6+1本。**2層設計**にして将来の分割はタグの束ね直しで吸収 | — | docs/09 §4 |
| D-020 | 内部タグ命名規則を確定。**アングル早見表18分類と対応。`tier-*` タグ必須** | — | docs/09 §5 |
| D-021 | トップページを **v3 構成12セクション**に改訂 | v2 の9セクション | docs/02 |
| D-022 | カテゴリーは**円形に差し戻し**(前セッションで四角に変更したものを撤回) | 2026-08-23 前半「カテゴリーを四角に」 | docs/02 §9 |
| D-023 | 「部屋で見る」に**スタイル別タブ**(ミッドセンチュリー / カラフルポップ韓国風 / 北欧 / ジャパンディ)と**セット購入ボタン**を追加 | v2 の単一ルーム | docs/02 §7 |

### 運用

| # | 決定 | 上書きした旧決定 | 記録先 |
|---|---|---|---|
| D-024 | **商品登録スキルを v2 化。リファレンスシート生成方式は廃止**(評価5/10・形状創作の構造的問題) | v1 の生成方式 | `.claude/skills/product-registration/` |
| D-025 | **形状の正は常に実物写真。** ブランドの見え方の正はリファレンスシート | — | 同上 |
| D-026 | 画像は3タイプ別パイプライン(サムネ=切り抜き+背景差替 / 商品画像=補正 / 説明画像=4点セット) | — | 同上 |
| D-027 | 画像棚卸しルーブリック3軸(文字量 / 役割 / ブランド適合)。**重複は役割ごとに最良1枚** | — | 同上 |
| D-028 | 鉄則維持: 価格確定までACTIVE禁止 / 形状・サイズを変える生成禁止 / 表現ルール遵守 | — | 同上 |
| D-029 | **「最短翌日お届け」は使用不可。** 海外取り寄せモデルで事実に反する。モックにあっても使わない | — | docs/02 §4 |
| D-030 | 画像・動画スキル(`image-prompt-director` / `seedance-scene-director`)を移植。**原本は変更禁止。MORU固有の追記は `references/moru-notes.md` 1ファイルのみ** | — | `.claude/skills/` |
| D-031 | **スキル本文より先に `moru-notes.md` を読むことを絶対ルール化**(CLAUDE.md ルール10)。原本から参照が張れないため、CLAUDE.md 側で担保する | — | CLAUDE.md |
| D-032 | 同名スキルがアカウント側(synced)にもあるが、**このリポジトリでは `.claude/skills/` 配下が正** | — | CLAUDE.md ルール10 / docs/06 §0 |

---

## 2026-08-23(夜)— 商品棚卸し8点への回答・価格ルール確定

オーナー決定。前セッションで挙げた未解決8点への回答を含む。

### 事業・価格

| # | 決定 | 記録先 |
|---|---|---|
| D-033 | **広告チャネルは Meta と TikTok Ads の2つのみ。TikTok Shop アフィリエイトは行わない**(以前からの確定事項だがログ未記載だった) | docs/00 / 広告運用 |
| D-034 | **フラワーラウンジは通常価格 ¥9,980 を維持。** 発売記念クーポン **20%OFF(期間明示)** で実売 ¥7,984 | docs/10 |
| D-035 | **取り消し線の二重価格表示は禁止**(実績なき比較価格のため)。**割引はクーポン方式のみ** | docs/10 / docs/01 §4 |
| D-036 | **登録時の値付けは概算方式**(送料区分3段 → 着地原価概算 ×3.0 → `X,980`/`X,480` 丸め → 20%OFF後に着地原価×2.4 を下回らないか検算) | docs/10 |
| D-037 | **仕入れ値 ¥500未満の小物は単品価格を付けず `tier-aov`**(同梱・クロスセル前提)。単品販売する場合の下限は ¥1,480 | docs/10 |
| D-038 | **本値付け(競合突き合わせ)は販売開始後にオーナーが適宜実施。** 登録時は概算で通す | docs/10 |
| D-039 | **CPA上限 = 限界利益の70%** | docs/10 |

### 構造・運用

| # | 決定 | 上書きした旧決定 | 記録先 |
|---|---|---|---|
| D-040 | **ナビ分類に「収納」を追加。6分類 → 7分類。** 内部タグ `storage` を新設。ヘッダーは「インテリア ▾」ドロップダウンに1行追加(トップレベル5項目は不変) | D-016(6分類) | docs/09 |
| D-041 | **`new-arrivals` の条件を「バリアント価格 > 0」から手動タグ `new-arrival` に変更。** 新着に出す商品だけ意図的にタグ付けする運用へ | — | docs/09 §4 |
| D-042 | **バリアントは AI が刈らない。** カラー以外のオプションを持つ商品を質問リストにしてオーナーが決める。崩壊した日本語オプション名の書き直しは構成確定後 | — | ops/products/ |
| D-043 | **`descriptionHtml` 内の既存 `<img>` 群は削除しない**(今後の画像生成の素材)。新しい説明文は**既存HTMLの上に追加**する | — | 商品登録スキル |
| D-044 | 説明文の分量は tier 別。**`tier-hero` / `tier-core` は docs/03 のフル構成**、それ以外は**リード文2〜3文(150〜250字)+ 仕様表 + 配送目安の定型文** | — | docs/03 |
| D-045 | **コレクションの英字タグ移行は「条件を新旧併記 → 商品タグ移行 → 件数照合 → 旧条件削除」の順で行う。** 商品タグを先に変えると接続が切れる(実際に一度切った) | — | docs/09 §4 |
| D-046 | **ペット商品の不足は解消**(オーナーが2点追加)。新規追加分も同じプロセスで棚卸しに含める | — | ops/products/ |

---

## 2026-08-23(深夜)— 商品命名の確定と命名規則の恒久化

| # | 決定 | 上書きした旧決定 | 記録先 |
|---|---|---|---|
| D-047 | **商品命名規則3行を確定。** ① 形式は `[愛称(hero/core/brand-builder のみ)] + [素材/モチーフ] + [カテゴリ名]`、`tier-aov` は説明形のみ ② **多色バリアント商品の名前に色名を使わない**(単色商品のみ可) ③ 愛称はカタカナ2〜3音で形・モチーフ・機能に由来 | D-036 系の暫定命名 | **docs/09 §8** |
| D-048 | **20件をリネーム。** CKB 連携名からの説明的な仮称を、愛称つきのブランド名に置換。handle も新 title の英訳スラッグへ統一 | — | ops/products/_classification_20260823.md |
| D-049 | **「フラワー」を MORU 初の公式シリーズとして確定。** フラワーラウンジ / フラワー キャットタワー / フラワー フロアクッション / フラワーバッド ナイトライト / フラワーバッド テーブルランプ の5点。内部タグ **`series-flower`** を新設。**スマートコレクションの条件には使わない**(広告・特集ページ用) | — | docs/09 §5・§8 |
| D-050 | **教訓を命名規則に組み込む。** ①「CKB連携名からの命名では色名が多色バリアントと矛盾しやすい」② 「説明形だけではブランドの棚にならない。hero / core / brand-builder は愛称必須」。**次の商品から自動で効くよう docs/09 §8 と商品登録スキルのワークフローに置いた**(スキルは命名ステップ 3-2 を新設。**オーナー承認前に title を確定しない**) | — | docs/09 §8 / `.claude/skills/product-registration/SKILL.md` |
| D-051 | **`productOptionUpdate` の `optionValuesToUpdate` は 1回 10件まで。** 11件目以降を渡すと超過分のオプション値と**そのバリアントが削除される**(実際にボア ラウンジチェアで 22 → 10 に消した。同一セッション内で全件復旧済み)。**オプション値を書き換えるときは 10件ずつに分割し、前後で `variantsCount` を照合する** | — | 本ログ / ops/products/_classification_20260823.md |

---

## 2026-08-23(深夜・続き)— バリアント構成の確定

> **D-042「AI はバリアントを刈らない」をオーナーが解除。** 「残るバリアントもブランド設計・戦略に
> 基づいて自動で判断してよい」との承認を受けて適用した。適用結果は `ops/products/_variant_decisions_20260823.md`。

| # | 決定 | 上書きした旧決定 | 記録先 |
|---|---|---|---|
| D-052 | **バリアントの取捨選択を AI の判断に委ねる**(ブランド設計・戦略に基づく)。**D-042 を解除** | **D-042** | ops/products/_variant_decisions_20260823.md |
| D-053 | **受注生産・ギフトボックス・仕入れ元の管理値は一律で外す。** 「カスタマイズ」5値 / ギフトボックス3値 / 「ほしい物リストに追加優先配送」等。**履行できない約束をカートに置かない** | — | 同上 |
| D-054 | **クッション類は「中材付き」に一本化し、柄を絞る**(8 / 8 / 5)。カバー単体は販売しない | — | 同上 |
| D-055 | **ブランケット2商品は統合せず両方残す。色は全色維持**(フェイクファー10 / フランネル11)、**サイズは3種に統一**(100×150 / 150×200 / 180×200)。**枕カバーは扱わない**(別商品化も見送り) | — | 同上 |
| D-056 | **判断の重い商品は `later` タグで据え置く。** アブストラクト オブジェ(21・中身がバラバラ)/ プラッシュ クッション(40・急がない)。`review-required` 2件と合わせ**4商品92バリアントは後日** | — | 同上 |
| D-057 | **オプション名・値の日本語化を完了**(D-042 後半)。仕入れ元の管理コード(`YL*D` `HD-Z` `S11-` 等)を全廃し、寸法表記を `幅◯×奥行◯×高さ◯cm` に統一。**未確認の「オーガニックレザー」は「レザー調」に言い換えた**(実態が確認できたら戻す) | — | 同上 |

**結果: 約600 → 325 バリアント**(うち後回し4商品で92)。**価格は変更していない。** 残った各バリアントの価格はそのまま有効。

---

## 2026-08-23(深夜・続き2)— フロント反映(ナビ7分類・新着の自動化)

| # | 決定 | 上書きした旧決定 | 記録先 |
|---|---|---|---|
| D-058 | **新着アイテムは「公開から60日以内」で自動判定する。** タグ `new-arrival` の手運用を廃止。`new-arrivals` は全商品・作成日降順のスマートコレクションにし、**60日の絞り込みはテーマ側**(`moru-product-grid` の `new_within_days` / 専用テンプレート `collection.new-arrivals.json`)。**タグの付け外しが不要になり、掲載期間が自動で切れる** | **D-041**(手動タグ運用) | docs/09 §2 |
| D-059 | **トップの「カテゴリーから探す」を docs/09 §1 の7分類に差し替え**(旧: 新着/猫のくらし/ペット/インテリア/雑貨/セール)。その下の商品グリッドは `cat-life`(1件)→ `pet` に | — | `templates/index.json` |
| D-060 | **ヘッダーのドロップダウンとモバイルのアコーディオンを実装。** どちらも **JS を足さず CSS と `<details>` だけ**で実現(CLAUDE.md 絶対ルール14「依存追加禁止・軽量優先」に従う) | — | `sections/moru-header.liquid` |
| D-061 | **Shopify の `main-menu` / `footer-shopping` を7分類に更新。** `main-menu` は 新着 / ペット / インテリア▾(すべてのインテリア+6分類) / セール。**「読みもの」は記事0件のため今回は入れない** | — | docs/09 §2 |
| D-062 | **API で作ったコレクションは販売チャネルに自動公開されない。** `storage` / `chair-stool` / `table` / `lighting` / `fabric` の5本が未公開のままフロントから見えなかった。**コレクションを作ったら必ず `publishablePublish` で公開し、`resourcePublicationsCount` を確認する** | — | 本ログ |

---

## 2026-08-23(深夜・続き3)— 商品登録ワークフローの確立

| # | 決定 | 上書きした旧決定 | 記録先 |
|---|---|---|---|
| D-063 | **商品画像を読み取って、寸法・素材・訴求を抽出する工程を正式にワークフローへ入れる。** 仕入れ元の販促画像には寸法図(「尺寸展示」)が入っていることが多く、「サイズ不明で書けない」の大半が解ける。**Shopify 側の画像には一切触らない(ローカルに落として読むだけ)** | — | **docs/11**(新設) / 商品登録スキル §4-2 |
| D-064 | **画像に書いていない寸法・素材は推定で埋めない。** 取れなければ仕様表に「確認中」と書き、**「確認できなかったもの」としてオーナーに報告する** | — | 同上 |
| D-065 | **画像にある禁止表現は書き写さない。** 実例: ランプ画像の**「护眼」「无蓝光」は健康に関わる未検証の主張**、テーブル画像の「隔水设计」は防水と誤読される。前者は不採用、後者は「こぼしても拭ける」という事実の範囲に言い換えた | — | ops/products/_tier1_batch_20260823.md |
| D-066 | **1商品に登録するのは6点**(`descriptionHtml` / `catch_copy` / `specs` / `features` / `title_tag` / `description_tag`)。**特徴は4〜5点**。全 tier 共通で**本文末に配送目安を1文入れる** | D-043(img 保持。オーナーが画像をメディア化したため役目を終えた) | docs/11 §3 |
| D-067 | **CKB → Shopify → フロントの一連の流れを docs/11 として独立文書化。** 実行の細部はスキル、全体像と落とし穴は docs/11 に置く | — | docs/11 |

---

## 2026-08-23(深夜・続き4)— 画像生成のルール改訂

> オーナーがリファレンスシート PNG 5枚を送付。**長らく「未受領」だった画像がリポジトリに入った。**

| # | 決定 | 上書きした旧決定 | 記録先 |
|---|---|---|---|
| D-068 | **リファレンスシートの PNG を画像生成の参照画像として実際に渡す。** 「読んで参考にする」ではない。プロンプトに**「これは MORU のブランドデザイン基準である」と明示**し、**同時にその商品の Shopify 登録済み商品画像も参照として渡す**(形状の正) | — | docs/11 §6 / 商品登録スキル §6 |
| D-069 | **「実物の切り抜き + 背景差し替えを優先」という指示を廃止。** 商品画像を参照として渡せば製品は再現されるので、工程の指示は不要。**欲しい絵を書く** | 商品登録スキル §6-A の旧記述 | 同上 |
| D-070 | **アクセントカラーを特定色に固定しない。** シート03 に「ブルー×テラコッタ」とあっても**踏襲しない**。カラフル・ポップに商品ごとに色味を変える。守るのは「差し色1〜2色 / 商品が主役 / 余白 / 明るい自然光」という**使い方**のほう | docs/01 §2 の「署名はMORUブルー×テラコッタ」は**ブランド全体では維持。生成画像のアクセントにだけ適用しない** | 同上 |
| D-071 | **生成枚数の目安: サムネ1枚(必須)/ 商品画像5枚程度 / 説明画像3枚程度。** 必要なら増やす。**バリアントが複数ある商品は各バリアントに合わせた画像を作れるとベスト** | — | 同上 |
| D-072 | **リファレンスシート PNG 5枚を収録。** `docs/brand/reference-sheets/01〜05_*.png`。**見た目の正は PNG。** テキスト版 `*.md` は検索用に併存させ、食い違ったら PNG が勝つ | README の「未収録」記載 | docs/brand/reference-sheets/README.md |

**まだ受領していないもの: `docs/mockups/home_v3_upper.png` / `home_v3_lower.png`。**

---

## 2026-08-23(深夜・続き5)— 画像生成の実装(Higgsfield 実地検証)

| # | 決定 | 上書きした旧決定 | 記録先 |
|---|---|---|---|
| D-073 | **画像生成はすべて Higgsfield で行う。モデルは `gpt_image_2`、quality は `low` 固定。** 他のツールを使わない | — | 商品登録スキル `references/higgsfield_recipe.md` |
| D-074 | **バリアント展開はサムネイルだけ。** 商品画像5枚・説明画像3枚はバリアント別に作らない。5色の商品なら **サムネ5 + 商品5 + 説明3 = 13枚**、1色なら9枚 | D-071(枚数の初版) | 同上 |
| D-075 | **プロンプトに「IMAGE 2/3 はブランド基準。レイアウト・文字・掲載商品を絶対にコピーするな」を必ず入れる。** 入れないとリファレンスシートそのもの(グリッド+日本語キャプション)を描き出す危険がある。`STRICTLY EXCLUDE` に text / lettering / watermark も必ず入れる | — | 同上 |

### 実地検証(コーデュロイ フォールディングチェア・サムネイル)

**成功。** 参照3枚(商品画像 + シート03 + シート01)を渡し、形状は完全に再現された
(クロームのチューブ構造・コーデュロイの畝・座面の厚み・グレーの脚キャップ)。
文字の混入なし、シートのコピーなし、白抜きなし。**アクセント色を指定しなかったところ、
モデルがマスタード系のラグと花瓶を選んできた**(D-070 と整合)。

**コスト実測: 0.5 クレジット/枚。**

---

## 2026-08-24 — 画像生成: アクセント・色温度・品質ゲート

| # | 決定 | 上書きした旧決定 | 記録先 |
|---|---|---|---|
| D-076 | **「差し色は1〜2色」だけでは不足。面積・彩度・置き場所まで指示する。** 色数だけ書くと、モデルは**画面のいちばん大きい面(商品の足元のラグ)を1色で塗り、商品より背景が目立つ。** ① 商品の真下・背後は**無地・低彩度**(オフホワイト / オートミール / 淡いグレージュ)② 差し色は**商品から離れた小物だけ**、画面の数パーセント ③ **商品より彩度の高いものを背後や足元に置かない** ④ **スクイントテスト**(目を細めて最初に目に入るのが商品でなければ作り直す) | D-070 を補強(色を固定しない方針自体は維持) | 商品登録スキル §6 / `references/higgsfield_recipe.md` / `image-prompt-director/references/moru-notes.md` / docs/11 §6 |

| D-077 | **生成画像の色温度をロックする(5200〜5600K / 午前の光 / 白は白のまま)。** ゴールデンアワー・夕方・ランプ光は禁止。名指しで避けるもの: 北欧 hygge の暖かさ / 韓国インテリア風の warm ベージュ / 秋色 / オレンジ寄りのハニーオーク床。**暖色を全部消すのではなく、全体にかかった暖色フィルターと夕方の光を消す。** あわせて **`AIRINESS`(抜け感)** ブロックを必須に | — | 商品登録スキル §6 / `higgsfield_recipe.md` / `moru-notes.md` / docs/11 §6 |
| D-078 | **スクイントテストを合否ゲートにしない。** 明らかに背景の色面が勝っているときだけ作り直し、**迷う程度なら通して次へ進む。** 最終判断はオーナーがまとめて行う。**チェックで作業が止まるほうが損失が大きい** | D-076 の「不合格なら作り直す」を緩和 | 同上 |
| D-079 | **リファレンスシート01・03 が暖色寄りであることを既知の課題として記録し、差し替え指定を残した。** 原因: 夕方寄りの主写真 / ハニーオークの床 / マスタード・テラコッタの大物 / **9色パレット自体が暖色多数で、寒色は MORUブルーとクロームグレーのみ(しかも生成では青アクセントを外した)**。**プロンプト側のロックは対症療法であり、根治はシートの差し替え** | — | `docs/brand/reference-sheets/README.md` |

**きっかけ(すべてコーデュロイ フォールディングチェアの検証でのオーナー指摘)**

- **D-076**: 足元の円形ラグ(v1 マスタード / v3 モスグリーン)が強く、**椅子が主役に見えなかった。**
  **色名の問題ではない**(モデルは毎回違う色を選んでおり、色を固定しない方針は機能していた)。
  **問題は面積と彩度で、そこを言語化していなかった**
- **D-077 / D-079**: 出力が**金色がかった夕方の光・ハチミツ色のオーク**に寄り、抜け感が出なかった
- **D-078**: チェックを厳しくしすぎると**作業が前に進まない**という指摘。品質ゲートは緩める

---

## 2026-08-24 — フェーズ転換: 画像制作をやめて売上に寄せる

> オーナー判断:「今やるべきことは各商品の画像をうまく生成することじゃなくて、
> **早くPDCAを回して商品を売れる環境を作って経験を積むこと**。ドロップシッピングを開始して、
> 広告を出して早く売上を立てたいフェーズ。一つひとつの商品にリファレンスシートに則って
> サムネイル・商品画像・説明画像まで作るのは、**正直やりすぎだった**」

| # | 決定 | 上書きした旧決定 | 記録先 |
|---|---|---|---|
| D-080 | **商品画像の作り込みワークフローを停止する。** サムネイル生成・生活シーン生成・画像棚卸し表・リファレンスシートを参照画像として渡す運用を全部やめる。**いま行う画像作業は「必須の中国語画像の日本語化」だけ**(寸法図 / 仕様表 / 使い方 / 同梱物。雰囲気カットは中国語のままでよい)。**崩れたら諦めて元のまま残す。元画像は削除しない** | **D-063 の画像部分 / D-068〜D-079 の運用**(内容は有効だが運用停止) | 商品登録スキル §5 / docs/11 §4 |
| D-081 | **旧ワークフローは捨てずに `docs/_archive/2026-08_image-production-workflow/` に退避。** レシピ(実測値・3回の失敗)/ スキル §5〜§6 / docs/11 §6 / シート改訂ブリーフ。**規模が出たあとに戻る可能性があるため** | — | 同フォルダの README |
| D-082 | **`image-prompt-director` / `seedance-scene-director` は退避しない。** Meta / TikTok の**広告クリエイティブ**で使う見込みがあるため、`.claude/skills/` に残す | — | CLAUDE.md 絶対ルール10 |

**この転換で優先順位が変わる。** 残作業の並びは
**① 説明文とメタフィールド(残り28商品)→ ② 必須画像の日本語化 → ③ 公開準備(docs/08)→ ④ 広告**。

---

## 保留(この改訂では決めなかった)

| 項目 | 決められない理由 |
|---|---|
| 信頼バッジ・配送目安の最終文言 | サンプル実測と CKB 配送条件が未確定 |
| フォントの最終決定 | 未決 |
| ベストセラーの暫定表記 | 販売実績が出るまでの表記を要決定 |
| ポイント制度(500pt)の実施可否 | 未決。Announcement 文言はプレースホルダー扱い |
| 送料無料しきい値(表示 ¥15,000 / ストア実設定 ¥7,700) | **どちらに寄せるかオーナー判断待ち。現状は虚偽表示** |
| `review-required` 2件(手編みコースター / レジン スカルプチャーオブジェ)の処遇 | オーナー回答待ち。それまで DRAFT 維持 |
| テーマ実装(v0.2 デザイン反映)の着手可否 | オーナー承認待ち |
| ヒーロー見出しの最終確定 | v0.2 プロンプトの表記が「コピー例」であり「確定」ではない |
| `cat-life` コレクションを `pet` に統合するか | 未決 |
| 「セール」ナビの遷移先 | 未決 |
| 「まとめて購入」の在庫切れ時の挙動 | 未決 |

---

## D-083 全商品の説明文・メタフィールドを登録し、確認できない項目は「確認中」と書いて台帳に積む(2026-08-24)

`later` / `review-required` の4件を除く全32商品に、説明文・catch_copy・features・specs・SEO を登録した。

- 寸法・素材・洗濯表示などが登録画像から読み取れないものは、**推測で埋めず商品ページに「確認中」と書いた。**
  買い手に見える形で「まだ分かっていない」と言うほうが、あとで訂正するより安い。
- 読み取れなかった項目は `ops/products/_unverified_report.md` に積んで、最後にまとめてオーナーへ報告する
  (オーナー指示「寸法が確認できないものに関しては、まとめて後でユーザーにその商品を報告すること」)。
- 仕入れ元の説明画像にあった**当店では履行できない表記**(1年质保 / 破损补寄 / 中国国内向けの返品条件)は
  一切引用していない。CLAUDE.md 絶対ルール15 のとおり。

## D-084 商品名・構成の疑義はこちらで直さず、台帳に上げてオーナー判断に回す(2026-08-24)

説明文を書く過程で、名前と中身が合っていないものが3件見つかった。
docs/09 §8 で命名変更にはオーナー承認が要るので、**こちらでは変えていない**。

| 商品 | 疑義 |
|---|---|
| フランネル ブランケット / フェイクファー ブランケット | 登録画像がほぼ同じ(縦畝のファー調プラッシュ)。仕入れ元だけが違う重複商品に見える。フランネルという名前も画像と合わない |
| パイピング クッションカバー | 中材付きで届くのに「カバー」 |
| ジオメトリック シェルフオブジェ | 実質ブックスタンド |

機械翻訳が残っていたオプション名(コイルの「2.5センチメートル* 34…」、ジオメトリックの「色分類」)は
名称ではなく明らかな不具合なので、承認を待たずに直した。

## D-085 仕入れ元の画像に混ざっている「顧客に出せないスライド」は削除せず台帳に上げる(2026-08-24)

商品メディアの中に、中国の広告法に関する免責声明、仕入れ元の B2B 営業スライド、
Taobao の URL 透かし、仕入れ元の社名ロゴが入っているものがある。
**CLAUDE.md 絶対ルール9(登録済みの商品画像を削除・差し替えしない)を優先し、こちらでは触っていない。**
一覧は `ops/products/_unverified_report.md`。

## D-086 照明2点は電気用品の法規を確認するまで売らない前提で扱う(2026-08-24)

- **ピボ テーブルランプ**: 口金が E27(日本の一般的な照明器具は E26)。説明文に「電球の規格は確認中」と明記。
- **ルナ ウォールライト**: 壁面直付けで配線工事が必要な器具。DRAFT のまま据え置き。

いずれも日本で電源につなぐ照明器具にあたるため、**PSE の扱いを確認しないまま販売しない。**

## D-087 商品名を2件変更(2026-08-24・オーナー指示)

| 旧 | 新 | 理由 |
|---|---|---|
| パイピング クッションカバー | **パイピング クッション** | 中材付きで届くので「カバー」は事実と違う |
| ジオメトリック シェルフオブジェ | **ジオメトリック ブックスタンド** | 実質ブックスタンド。何に使うものか名前で分かるようにする |

handle も `piping-cushion` / `geometric-book-stand` に変更した。説明文・SEO も追随。

## D-088 中国語画像の日本語化は「読めないと買えない画像」だけ。20商品27枚で完了(2026-08-24)

`docs/11 §4` の基準どおり、寸法図・仕様表・使い方を日本語化した。**20商品・27枚**。
**元画像は1枚も削除せず、日本語版を追加している**(CLAUDE.md 絶対ルール9)。
対象商品と、この作業で判明したことは `ops/products/_unverified_report.md`。

オーナー指示により **ピボ テーブルランプとルナ ウォールライトは対象外**。

手順で効いたこと(スキル §5 に反映済み):

- **元画像の縦横比に近い aspect_ratio を選ぶ。** ここを外すと図が潰れる
- **プロンプトに訳語を名指しする。** 「中国語を日本語に」だけだと「茶几」「储物柜」が残る
- **維持するもの(寸法の数値・寸法線・レイアウト・背景色)を全部書き出す**
- **透かしと、中国国内向けの取引条件は、この機会に消す/差し替える。**
  他社商標と履行できない条件を自店の商品ページに出さないため

## D-089 日本語化で判明した仕様の食い違いは、仕様表のほうを直す(2026-08-24)

- **セル サイドボード**: 仕入れ元の画像に「整装发货免安装」。仕様表の「組み立て式」が誤りだったので
  「完成品でのお届け(組み立て不要)」に修正
- **デュオ ナイトテーブル**: 寸法図が見つかり「確認中」を解消(1連 45×40×52cm / 2連 90×40×52cm)
- **セラミック ブックエンド**: 同上(1個 高さ16.7×幅9cm・2個1組)



## D-090 説明画像は「全数目視」で走査する。途中打ち切りをしない(2026-08-24)

1回目の日本語化は説明画像を途中までしか見ずに打ち切っていたため、
**クラウド サイドテーブルの寸法図を「1枚もない」と誤報告**した(実際は存在した)。

再発防止として、走査は次の手順に固定する:

1. 全商品のメディア URL を一覧化する(`media` を全件ページング。今回は 589 枚)
2. **20枚ずつのコンタクトシート(5×4・1セル380px)に落として全数を目視する。**
   バイト数/画素の閾値で「図っぽい画像」を絞り込む方式は**取りこぼす**(実測 0.24〜0.45bpp で写真と分離できない)
3. 見つけた図をすべて訳す。「もう十分」で止めない

この再走査で **13枚を追加**し、9商品の仕様表と説明文を実数に更新した(内訳は
`ops/products/_unverified_report.md`)。

## D-091 日本語版の画像はメディアの4〜7枚目に置く(2026-08-24)

`productCreateMedia` は末尾に追加するため、初回は日本語版が写真20〜30枚の後ろに埋もれ、
オーナーから「日本語化の画像はいって無くない?」と指摘された。

**ルール: 日本語化した図はギャラリー写真の直後(4〜7枚目)に置く。**
`productReorderMedia` の `moves: [{ id, newPosition }]` で移動する
(`newPosition` は **0始まりの文字列**)。手順はスキル §5 に追記済み。


## D-092 価格再設計の目的を「割引前提をやめる」に確定(2026-08-24)

オーナー確定:

> **現行の ×3.0 機械値付けを全商品再監査し、割引を前提にしなくても
> 通常価格そのものに納得感がある価格へ再設計する。**

前提の確認:

- **`compare_at_price` は全36商品 null。ストアに30%OFF の二重価格表示は存在しない。これを正とする**
- 懸念されているのは**表示ではなく値付け倍率**。広告クリエイティブの話ではない

その上で、**multi-buy 対象商品だけ** 1点=割引なし / 2点=10%OFF / 3点以上=15%OFF を検討する。
**家具全体には適用しない。** `tier-aov` / `cushion` / `textile` / `object` / 小型雑貨を中心に、
**商品ごとに適性を判断する。**

指示書は `docs/12_price_redesign_and_multibuy.md`。**Owner 承認まで価格・タグ・metafield・
discount を変更しない。**

## D-093 Variant 別 CKB 原価を再取得する。商品単位1原価で価格監査を確定しない(2026-08-24)

棚卸し `ops/products/_inventory_20260823.md` は**商品あたり1原価**しか持っていない。
サイズ・仕様差の大きい商品では、これが最安 Variant の値である可能性が高い。

**オーナー承認のもと、Variant / SKU 単位で原価を取り直す。**
優先: デュオ ナイトテーブル / セル サイドボード / セル モジュールキャビネット /
ピボ テーブルランプ / ボア ラウンジチェア / ハル ダイニングチェア / ブランケット2種。
原価未記録の フラワー キャットタワー / ソラ キャットハンモック は CKB から新規取得する。

記録項目は **CKB商品原価 / 中国国内送料 / 代行・検品等 / 国際送料 / 関税・輸入消費税等 / 着地原価**。
**不明項目は推測せず「未確認」。**

**現時点で算出されている ×7〜×10 の倍率は Variant 原価欠落による可能性が高いため、
「値下げ推奨」の根拠に使わない。**

## D-094 テーマの作業対象を MORU Frontend Dev に固定。MAIN は書き込み・publish 禁止(2026-08-24)

2026-08-24 の実査結果:

| テーマ | id | role | 扱い |
|---|---|---|---|
| **MORU Frontend Dev** | `166341181680` | UNPUBLISHED | ✅ フロント作業の宛先 |
| MORU LIVING (Skeleton構築) | `166203621616` | **MAIN** | 🚫 write / publish 禁止 |
| Horizon | `166127468784` | UNPUBLISHED | 旧プロジェクト。触らない |
| Development (87442c-vm) | `166203523312` | DEVELOPMENT | CLI の一時テーマ |

- **MAIN への書き込みを行わない。`--allow-live` を使わない**
- **どのテーマも publish しない**
- push 前に毎回 `themes(first: 20) { nodes { id name role } }` で role を確認する
  (**id は入れ替わりうる。名前ではなく role で判定する**)

2026-08-23 までの docs には `--theme 166203621616 --allow-live` で push した記録が残っているが、
**それは当時の運用で、現在は無効**。handoff 冒頭・docs/07 §2・CLAUDE.md を現運用に合わせて更新した。


## D-095 Frontend は ChatGPT 側の担当。Claude Code は theme files を変更しない(2026-08-24)

Shopify Frontend の実装・検証を **ChatGPT 側が並行して進めている。**
作業テーマは **MORU Frontend Dev**(`166341181680` / UNPUBLISHED)。

| 担当 | 範囲 |
|---|---|
| ChatGPT | Frontend Dev の theme files(数量アップセルUI / Cart Drawer / Cart Page) |
| **Claude Code** | Shopify 側のデータ(商品・価格・**Discount Function**)と docs |

**Claude Code は Frontend の theme files を変更しない。** 競合させない。
MAIN への write / publish 禁止は D-094 のとおり。

### ChatGPT 側で実装済み(2026-08-24 時点)

- 商品ページの数量アップセル UI(1点 / 2点 / 3点以上)。
  2点は1点目・2点目を個別選択、3点以上は `+1` で増やせる。**Variant 混在可**。
  想定 10% / 15% OFF 価格を**参考表示**する
- 複数 Variant は `cart/add.js` でまとめて追加
- Cart Drawer / Cart Page に、**実際に Discount が適用された場合の**
  通常価格(取り消し線)・割引後価格・「¥○○○ お得」・通常合計 / 割引合計 / 割引後合計 の表示

### 現状と、Claude Code の担当

**Discount Function が存在しないため Cart / Checkout の実価格は通常価格のまま。**
商品ページの割引表示は参考値。**Frontend 側で擬似的に価格を書き換えない。**

Claude Code は **Discount Function を作り、有効化後に Frontend Dev の表示価格と
Cart / Checkout の実価格が一致するかを検証する。**
不一致が Frontend 側にある場合は、**直さずに指摘としてまとめて ChatGPT 側へ渡す。**

## D-096 Variant 別 CKB 原価は、この実行環境からは取得できない。Owner 依頼に切り替える(2026-08-24)

D-093 で承認された「Phase A の最初の実作業」は**未完了。理由は権限でも手順でもなく到達性。**

| 経路 | 実査結果 |
|---|---|
| Shopify `inventoryItem.unitCost` | **全257 Variant で null** |
| Variant metafield | **0件**(Variant 側に metafield 自体が無い) |
| Product metafield | `custom.catch_copy` / `features` / `specs` / `swatch_colors` / SEO 2件のみ。原価系は無い |
| `inventoryItem.measurement.weight` | **全 Variant 0kg**(送料区分を実重量で検証することもできない) |
| `www.ckb.jp` / `ckb.jp` | 到達不可(DNS は解決するがプロキシで失敗。`example.com` 等は 200 なので環境全体の遮断ではない) |
| `detail.1688.com` | 到達不可 |
| CKB 認証情報 | repo にも環境変数にも無い |

**過去の Shopify 側にも残っていない。** 取り込み直後は「価格欄 = CKB 仕入れ値」だったが
(`_classification_20260823.md` 末尾)、その後の値付けで**全 Variant の price が上書きされ、
Admin API に価格履歴は無い**。`_inventory_20260823.md` が商品あたり1値しか持っていないのは、
**その1値しか記録に残さないまま上書きしたから**であって、取り直せば出るものではない。

→ **Owner が CKB 画面から転記する形に切り替える。**
依頼票 `ops/products/_ckb_cost_request_20260824.md`(優先度1: 11商品62 Variant /
優先度2: 10商品113 Variant)。

**教訓: 仕入れ値を price 欄に入れたまま値付けで上書きしない。**
上書きする前に Variant 単位で ops に控える(docs/10 §5 は「商品ごとに記録する」と
書いてあったが、`ops/products/{分類}-{商品}/product.md` は**1件も作られていなかった**)。

## D-097 docs/12 §6 の ×7〜×10 は値付けではなく原価記録の取り違えだった。値下げ推奨は0件(2026-08-24)

`_variant_decisions_20260823.md` の削除ログと突き合わせた結果、
**高倍率10商品のうち9商品で「記録原価が、いま売っていない Variant のもの」と説明がついた。**

ピボ = 削除した「カスタマイズ仕様」/ クッション3種 = 廃止した「カバーのみ」/
ガラスフラワー = 削除した「単品1本」(現行は全て7点セット。¥70×7 なら ×3.09 で式どおり)/
ボア = 削除した「ラテックス座面」/ セル2種 = 削除した部材・カスタマイズ。

**したがって値下げ推奨は0件。**「高倍率だから下げる」は根拠にならないという Owner の指示は正しかった。
**唯一 フラワー フロアクッション(×6.08)だけが Variant 無変更で説明がつかない。最優先で実測する。**

同時に、**docs/12 §6 が最小 Variant 価格しか見ていなかった**ことも判明した。
実際は**11商品で Variant ごとに価格が違う**。とくに **アブストラクト オブジェは
¥4,980〜¥58,980 の21種**(稲の耳のオブジェ / 陶器コーヒーカップ / ラッキーキャットのペア …)が
1商品に同居しており、**価格以前に商品構成の判断が要る。**

監査の全文は `ops/products/_price_audit_20260824.md`、Variant 単位257行は
`_price_audit_20260824_variants.csv`。**Shopify 側は一切変更していない。**

## D-098 まとめ買いの対象判定は専用タグではなく Product metafield にする(2026-08-24・提案)

`custom.multi_buy_eligible`(boolean)。**Owner 承認前なので、まだ作っても付けてもいない。**

理由は docs/12 §7-5 の懸念そのもの。`tier-*` は階層、`cushion` / `object` / `textile` は
カテゴリ(docs/09)なので、**販促の可否フラグを相乗りさせるとカテゴリを変えた瞬間に
割引対象が動く。** metafield なら Function の input から直接読め、タグ設計を汚さない。

Function は **fail-closed**(metafield が無ければ対象外)。
**metafield を1件も付けないまま有効化しても割引は1円も出ない。**承認ゲートとして意図的にそうした。

判定は **推奨13 / 要確認6 / 対象外17**。家具は原則対象外。
**ハル ダイニングチェアは「2脚セット」Variant があるため、数量割引と二重になる**という
構造的な理由で対象外(デュオ ナイトテーブルの「2連」も同様)。

## D-099 送料無料しきい値の不整合は、数量割引より先に片づける(2026-08-24・要 Owner 判断)

実査でストア設定は **¥7,700**(`deliveryProfiles` の TOTAL_PRICE 条件)。トップ表示は ¥15,000。
**現状は虚偽表示。**

さらに **しきい値は割引後の小計で判定される**ため、数量割引を入れると
「¥8,000 のカゴが10%OFFで ¥7,200 になり送料無料を失う」という体験が生まれる。
**一本化を先に済ませる。**

併せて判明した事実: **ストアに Discount は1件も存在しない**(`discountNodes` が空)。
docs/10 §3 が「現行: フラワーラウンジ 発売記念クーポン20%OFF」と書いているのは事実と違い、
docs/10 §4 の「クーポン併用前提で CPA上限 ¥3,196」という前提も現状と合っていない。
**docs/10 の改訂は承認後に行う(docs/12 §10 のとおり先回りしない)。**

## D-100 Discount Function を deploy した。ビルドが止まっていた原因は javy ではなくビルド設定の無限再帰(2026-08-25)

**deploy は成功した。** `MORU Promotions` の app version `moru-promotions-3` が active。
Discount は作っていない(`discountNodes` は0件のまま)。

前セッションが「javy のダウンロードが遅い」と見立てていたハングは、**ダウンロードではなかった。**
`shopify.extension.toml` の `[extensions.build] command = "npm run build"` が
`extensions/multi-buy-discount/package.json` の `"build": "shopify app function build"` を呼び、
それが同じ build command をまた呼ぶ。**プロセスが入れ子に増え続け、出力も出ないまま永久に止まる。**
`ps --forest` で入れ子が見えて判明した。

直したこと(いずれも挙動ではなく構成の問題):

| # | 症状 | 原因と対処 |
|---|---|---|
| 1 | 出力ゼロで永久ハング | 上記の再帰。**JS Function は Shopify CLI(Javy)が直接ビルドするので build command は空でよい。** package.json の `build` スクリプトも削除した |
| 2 | 「build command が無い」で落ちる | **JS Function のエントリは `src/index.js` に固定**。無いと `isJavaScript` が false になる。`src/index.js` から named export を再輸出するだけにした(ロジックは不変) |
| 3 | `Invalid export names` | TOML の `export` は Wasm Component Model の制約で **kebab-case 必須**。`cart-lines-discounts-generate-run`。JS 側の camelCase とは CLI が自動で対応づける |
| 4 | `graphql-code-generator` が落ちる | CLI は JS ビルド時に必ず typegen を走らせる。この Function は生成型に依存しないので `typegen_command = "true"`(no-op)にした |
| 5 | CLI が外に出られない | **この実行環境の Shopify CLI はプロキシを読まない。** `NODE_USE_ENV_PROXY=1` と `NODE_EXTRA_CA_CERTS=/root/.ccr/ca-bundle.crt` を付けないと Shopify への通信が無言でハングする |

**ストアへの反映は確認済み。** 管理画面のアプリ詳細で
Functions が **「利用可能: ディスカウント」**、アクティブ **0件**(= Discount 未作成)。意図どおり。

**Admin API の `shopifyFunctions` では確認できない。**
このクエリは「問い合わせているアプリ自身が持つ Function」しか返さない
(*Returns Shopify Functions owned by the querying API client installed on the shop*)。
Claude Code の Shopify MCP は別アプリなので**常に0件になる。**
2026-08-25 にこれを「未インストールの可能性が高い」と誤判定した。**同じ誤判定をしないこと。**
確認は管理画面 → 設定 → アプリと販売チャネル → MORU Promotions → Functions で行う。

## D-101 日本国内の送料無料は ¥7,700 を正式値とする(2026-08-25・Owner 決定)

**D-099 の「¥7,700 と ¥15,000 のどちらに寄せるか」は決着した。**

- **正式値は ¥7,700。** ストアの `deliveryProfiles`(国内配送 / TOTAL_PRICE ≧ 7700 で送料0)が
  元から ¥7,700 なので、**Shopify の配送設定は変更していない**
- テーマに残る ¥15,000 表記は**不整合**として扱う。修正は Frontend(ChatGPT)担当。
  該当箇所の全件は `ops/_launch_blockers_20260825.md` §C
- **DEV テーマは既に ¥7,700 に直っている**(アナウンスバー / PDP 信頼バッジ / ホーム USP)。
  **未修正なのは `locales/*.schema.json` の既定値4箇所**。これはセクションを追加し直したときに
  ¥15,000 が復活するので、放置してはいけない

### 割引が送料無料を壊す件(D-099 で挙げた問題)は**残っている**

しきい値は**割引後の小計**で判定される。危険帯:

| 割引 | 割引前の小計がこの範囲だと割引後に ¥7,700 を割る |
|---|---|
| 10%(2点) | ¥7,700 〜 ¥8,555 |
| 15%(3点以上・ペア) | ¥7,700 〜 ¥9,058 |

推奨13商品だけのカゴで実際に起きるのは **6パターン(全て ¥7,960 → ¥7,164)**。
割引 ¥796 を受けて送料 ¥870 を失うため**実質 ¥74 の損**になる。
ペアでは **P-1(ベルベット+フランネル)の最小構成 ¥8,960 → ¥7,616** が該当。

**対処はまだ決めていない。**選択肢は (a) しきい値を下げる (b) Delivery Customization Function で
割引前の小計を見る (c) カートで「あと ¥536 で送料無料」を出す (d) 許容する。
**新しい送料無料金額を勝手に設定していない。**

## D-102 まとめ買いの対象判定は metafield、SALE の対象判定はタグ。方式を分ける(2026-08-25)

D-098 のとおり **まとめ買いは `custom.multi_buy_eligible`(boolean metafield)**。
**2026-08-25 に定義を作成した**(`gid://shopify/MetafieldDefinition/258121728240`)。
**商品への true / false 設定は CKB 原価監査の後。まだ1件も設定していない。**

一方 **SUMMER SALE の対象判定はタグ(`sale`)** にする。理由が違う:

| | 判定方法 | なぜ |
|---|---|---|
| まとめ買い / ペア | **metafield / Discount の設定 JSON** | Function の input から読む必要がある。タグは Function から読みにくく、カテゴリタグと混ざる |
| SUMMER SALE | **タグ `sale`** | Shopify 標準の Automatic Discount が**コレクションを直接指定できる**。Function が要らない。<br>スマートコレクション(rule: TAG=`sale`)と割引対象が**タグ1つで同時に動く** |

**この使い分けは意図的。**「販促フラグをカテゴリタグに相乗りさせない」という D-098 の原則とは
矛盾しない(`sale` は階層でもカテゴリでもない独立した販促軸)。

## D-103 PAIR IT YOUR WAY は固定 Bundle Product を作らず、組み合わせ割引 Function で実装する(2026-08-25・Owner 決定)

**理由: CKB の SKU / 在庫 / fulfillment 構造を増やさないため。**

`apps/moru-promotions/extensions/pair-set-discount/` を新設して deploy した
(`moru-promotions-4` が active / extension uid `54593006-b244-854c-5c79-1dca16afacb8f0c73de2`)。

- **Product ID をコードに書かない。**対象グループ・割引率は Discount の app metafield
  (`$app` / `function-configuration`)の JSON から読む。組み合わせを変えても再デプロイ不要
- 判定は **Product ID 単位。Variant は問わない**
- 成立 = グループの `productIds` が**全部**カートにある
- `setCount`(= 各 Product の合計数量の最小値)で頭打ちにする。
  **「A×10 + B×1 で全部15%OFF」にならないようにするため**
- 1商品が複数グループに属しても**割引は1つだけ**。率が高いほうが勝つ
- **fail-closed。**設定が無ければ割引0円
- テスト **21 passed / 0 failed**

**Discount resource は作っていない。有効化もしていない。**
組み合わせ候補8案は `ops/products/_pair_candidates_20260825.md`。

## D-104 Discount の combination は全て off で始める(2026-08-25)

まとめ買い / ペア・セット / SUMMER SALE のいずれも、
**product / order / shipping discount との自動スタッキングを全て off** で作成する。

理由: ペア15% と まとめ買い15% が重なると **30%相当**になり、原価割れの危険がある。
ストアに他の Discount が0件なので、**off から始めて実績を見てから開ける**のが安全。

## D-105 ローンチ阻害は BLOCKER 15件。Owner にしかできないものが5件ある(2026-08-25)

全件は `ops/_launch_blockers_20260825.md`。**Owner 待ちのクリティカルパス:**

1. **在庫**。36商品中35商品が `totalInventory` = 0。この状態では公開できない
2. **5ページの公開**(特商法・プライバシー・利用規約・FAQ・About)。**法令上必須のものを含む**
3. **返品ポリシーを1つに決める。**表示は「30日返品」、本文は「不良・誤配送のみ7日以内」で矛盾している
4. **「新規会員登録で¥500ptプレゼント」の実体確認。**ポイント制度をストアに確認できなかった
5. **「2年保証」の実体確認。**保証主体と範囲の文書が無い

**注文は0件**(`ordersCount` = 0・EXACT)。したがって:

- ホームの「**ベストセラー**」は手動で選んだ5商品に順位を振っているだけで**販売実績の裏付けが無い**
- PDP の「**よく一緒に購入されています**」も同じ。参照先の `cat-life` は1商品しか入っていない
- カートの `review` ブロック(星5・「検証済みの購入者」)は**偽レビュー。DEV・MAIN の両方にある**

いずれも docs/01 §4「その表示が事実か」と絶対ルール6に反する。**Frontend 側の修正が要る。**

## D-106 DEV → MAIN のテーマ反映は単純上書きできない。allowlist を3段に分けた(2026-08-25)

全件は `ops/theme/_dev_to_main_20260825.md`。**MAIN には1バイトも書いていない。**

差分は **同一59 / 変更18 / DEV のみ7 / MAIN のみ1**。実査で分かったこと:

1. **JSON テンプレートは DEV が完全な上位集合。**サイズが小さく見えるのは DEV 側が JSON を
   圧縮して書いているだけで、内容は MAIN を全て含む。**安全にコピーできる**
2. **`.liquid` で DEV のほうが小さいものは、本当に中身が減っている。**
   `moru-product-details.liquid` は MAIN のレビュー UI 一式と `@app` ブロック対応が**丸ごと無い**
3. **`locales` は4ファイルすべて MAIN のほうが大きい。**上書きすると `t:` キーが消えて翻訳が壊れる。
   **マージが要る**
4. **`moru-header.liquid` は MAIN のほうが新しい。**DEV は v2 に乗り換えたので v1 を更新していない。
   **コピーすると MAIN を退化させるだけ**

→ **Tier 1(そのままコピー可・11ファイル)/ Tier 2(依存を満たしてから・1)/
Tier 3(Owner が中身を見て決める・6)/ 除外(7)** に分けた。

⚠️ **Shopify MCP の `graphql_mutation` は MAIN(live)への書き込みをブロックする。**
反映には Shopify CLI のテーマ認証が要るが、**この実行環境にはまだ無い。**
Owner 承認が出た時点で用意していただく必要がある。

## D-107 【訂正】「36商品中35商品が在庫0」は誤りだった。全商品が購入可能(2026-08-25)

**D-105 の在庫 BLOCKER を取り下げる。**

`product.totalInventory` が 0 と返るのを見て「在庫0で公開できない」と報告したが、
**`totalInventory` は在庫を追跡している商品しか集計しない。**

実測(全 257 Variant・3ページ取得・SKU を既存台帳と完全一致検証):

| 指標 | 値 |
|---|---|
| `availableForSale = true` | **257 / 257** |
| `inventoryItem.tracked = false` | **255 / 257** |
| `tracked = true` | **2**(MORU フラワーラウンジのみ・各5点) |
| `inventoryPolicy` | 全件 DENY(**追跡していないので実質働かない**) |
| Location | Shop location 1箇所(`fulfillsOnlineOrders: true`) |

**在庫はローンチ阻害ではない。**購入できないのは **DRAFT の4商品(42 Variant)だけ**で、
これは在庫ではなく公開状態の問題。詳細は `ops/products/_inventory_audit_20260825.md`。

### PDP の在庫表示は壊れない(実装を確認した)

`sections/moru-main-product.liquid`(DEV・MAIN・ローカルで checksum 完全一致)は
`if current_variant.inventory_management != blank` で**追跡なし商品の数量を読まない**。
`stock_display: "inventory"` でも在庫ラベルが出ないだけで、異常な数字は表示されない。

### ただし別の問題が見つかった

`inventory_quantity` に **CKB の在庫数がそのまま入っている**(最大 773,835)。販売判定には
使われないが、**0 が入っている Variant が3件**ある(マッシュルーム1・フラワー フロアクッション2)。
追跡していないので Shopify は普通に売る。**発注できるか確認が要る。**

## D-108 配送設定に2件のローンチ阻害がある(2026-08-25・実査)

| # | 内容 |
|---|---|
| 1 | **国際配送ゾーンが有効で、条件が「総重量 0〜2kg で ¥3,000」。**一方 **全商品の重量が 0kg**(セル サイドボード・デュオ ナイトテーブルで実測)。つまり **¥92,480 のキャビネットが国際送料 ¥3,000 で注文できる。赤字確定** |
| 2 | **国内配送に条件なしの「速達 ¥3,762」がある。**実際の納期は海外取り寄せで2〜3週間。**速達に見合う実態が無い**(CLAUDE.md 絶対ルール5) |

**どちらも設定変更していない。Owner 承認が要る。**

## D-109 ポリシー文書が「プライバシーのみ」。返金ポリシーが存在しない(2026-08-25・実査)

`shop.shopPolicies` にあるのは **PRIVACY_POLICY だけ**。しかも Shopify の既定テンプレートのままで、
差し込みの `{{ phone }}` が**空**(ストア設定の電話番号が未入力)。

**存在しないもの: 返金ポリシー / 利用規約 / 配送ポリシー / 連絡先情報。**

さらに:

- **6ページすべて `body` が空。**中身はテーマのテンプレート側にある
- **プライバシー・利用規約のページは見出しだけで本文が無い。公開すると白紙が出る**
- **特商法は13項目中9項目が空**(販売業者・運営統括責任者・所在地・電話番号・メールアドレス・
  URL・商品代金以外の必要料金・支払方法・支払時期)。**すべて法定必須**
- **Contact ページが `templateSuffix: "contact"` を指しているが、`templates/page.contact.json` が
  DEV・MAIN のどちらにも存在しない。**唯一の公開ページなので実機確認が要る

**法務文面は推測で作らない。**足りない項目を挙げただけ。詳細は `ops/_pages_policy_audit_20260825.md`。

## D-110 「30日返品」の裏付けは1つも無い。実態は「7日・不良/誤配送のみ」(2026-08-25)

ストアに存在する文書はすべて **「到着後7日以内・不良品または誤配送のみ」** で一致している
(特商法ページ / FAQ / PDP アコーディオン)。**Shopify の返金ポリシーは存在しない。**

一方 Frontend は **「30日間の返品・交換」「イメージ違いでも安心の30日返品保証」「30日返品対応」**
を Homepage USP / PDP 信頼バッジ / Cart page に出している(DEV にも残存)。

**同じサイトの中で正反対のことを言っている。**選択肢:

- **A. 実態を「7日・不良/誤配送のみ」にする** → Frontend の30日表示を消す + 返金ポリシーを作る
- **B. 「30日・お客様都合可」を運用する** → 文書側を全て書き換え + 返送料負担者を決める +
  **返品コストが原価計算に効く**(docs/10 §4 の返品コストは現在 0(未確認)で置いてある。前提が変わる)

**どちらを採るかは Owner の事業判断。文面はこちらで作らない。**

## D-111 ペア採算の判定ツールを用意した。原価が入り次第すぐ回せる(2026-08-25)

`ops/tools/pair_audit.py` を新設。`price_audit.py` の出力と
`ops/products/_pair_definitions.csv` を読んで、ペアの15%OFF採算を出す。

- 定価合計 / 割引後 / 着地原価 / 倍率 / 粗利額 / 粗利率 / **限界利益 / BE_ROAS / CPA上限**
- **送料無料 ¥7,700 を割る組み合わせを自動で 🚩 表示する**
- **片方でも原価が未入力なら「原価待ち」。推測で埋めない**

現在の定義は Owner 指定の **P-8(ソラ キャットハンモック × フェイクファー ブランケット)**と
**P-6(クラウド サイドテーブル × マッシュルーム コードレステーブルランプ)**の2組。
実行済み・両方とも「原価待ち」で正しく止まることを確認した。

`price_audit.py` は既に landed cost / current price / current multiple / proposed everyday price /
gross margin / 10%・15% margin / 限界利益 / BE_ROAS / CPA上限 を算出する。
**追加実装は不要。原価 CSV が入ればそのまま回せる。**

## D-112 着地原価の採用順を決めた。CKB商品原価223件を反映(2026-08-25)

Owner が CKB 商品原価を 223 Variant 分回収。取り込み時に**台帳とキー完全一致を検証**
(欠落0・余剰0)。未取得は **34 Variant / 3商品**(アブストラクト オブジェ21 / ルナ ウォールライト8 /
セル モジュールキャビネット5)。**推測で補完していない。**

### 着地原価の採用順(`price_audit.py`)

1. `landed` 実測値がある → **実測**(現在 0 件)
2. 実測なし + `ckb_cost` あり → **`landed_est = ckb_cost + ship_est`**
3. `ckb_cost` も無い → **原価待ち**

**`ship_est` は実送料ではない。**台帳の**価格設計用の送料概算**
(小物 ¥800 / 中型 ¥1,750 / 大型 ¥3,500)。国内送料・代行手数料・国際送料・関税の
実測値はまだ1件も無い。**`cost_source` 列に「実測 / 推定 / 原価待ち」を1行ごとに記録する。**

**`_ckb_costs.csv` の `ckb_cost` は CKB の商品原価そのままで保存する。**
送料概算を足して上書きしない。`landed_est` は監査ツール側で毎回生成する。

⚠️ **`ship_est` の精度がそのまま判定の精度。**低単価商品ほど影響が大きい
(ブランケット ¥324 の原価に ¥800 の送料概算 = 原価の71%が概算)。

### 既存バグを1件修正した

`'要検討: 15%OFF後 ×%.2f' % m15` の **`%O` が書式指定子として解釈されて `ValueError`**。
**原価が入って初めて到達する行**だったため、これまで表面化していなかった。f-string に置換。

商品単位サマリ生成に `ops/tools/product_summary.py` を新設。

## D-113 🔴 docs/10 の「×3.0」と docs/12 の「15%OFF後 ×2.6」は両立しない(2026-08-25)

```
×3.00 × 0.85 = ×2.55  < 2.6
```

**「着地原価 ×3.0 で値付けする」というルールどおりに作った商品は、15%OFF を出した瞬間に
必ずスクリーニングを割る。**個別商品の問題ではなく**ルール同士の矛盾**。

| 割引 | ×2.6 を保つのに必要な通常倍率 |
|---|---|
| 15%OFF | **×3.06 以上** |
| 10%OFF | **×2.89 以上** |

これが「要検討13商品」の正体。**全て ×3.02〜3.06 に張り付いているだけで、赤字ではない**
(粗利率はいずれも 61% 台)。

対処案: **A. 15%OFF を出す商品だけ ×3.06 以上にする(ほぼ全て +¥500)** /
B. スクリーニングを ×2.55 に下げる / C. 上限を 15% → 12% にする / D. 放置。
**推奨は A。**家具にまとめ買い15%を出す予定が無いので、**実際に効くのは
セラミック ブックエンド / ベルベット クッション / ソラ キャットハンモック の3件だけ。**

**Owner 判断待ち。価格は1件も変更していない。**

## D-114 ソラ キャットハンモックだけが ×3.0 を割っている。PAIR 第一候補が入れ替わる(2026-08-25)

**33商品中、通常価格で基準(×3.0)を割っているのは ソラ キャットハンモックのみ(×2.42)。**
CKB原価 ¥4,973 + 大型送料概算 ¥3,500 = ¥8,473 に対して ¥20,480。

**これで PAIR の第一候補がひっくり返った。**

| ペア | 15%OFF 倍率 | 粗利率 | 限界利益 | 判定 |
|---|---|---|---|---|
| **P-6**(クラウド サイドテーブル × マッシュルーム ランプ) | **×2.62** | 61.9% | ¥11,375 | ✅ **合格** |
| P-8(ソラ キャットハンモック × フェイクファー ブランケット) | **×2.12** | 52.9% | ¥10,036 | 🔴 基準割れ |

**P-8 が落ちた原因はペアではなく、ソラ単体が既に基準を割っていること。**
**ソラを ¥25,980 にすれば P-8 は ×2.61 で成立する。**

→ **第一候補を P-6 に変更することを推奨。**P-8 はブランド適合(Pet-first, Interior-always.)が
上なので、**値上げが通れば本命に戻せる。**

⚠️ **P-6 の2商品は SUMMER SALE 候補にも入っている。**ペアにするなら SALE から外すのが素直。

## D-115 上振れ3商品は据え置きを推奨する(2026-08-25)

| 商品 | 現倍率 | ×3.0 の機械案 | 推奨 |
|---|---|---|---|
| フラワー フロアクッション | ×5.01–5.37 | ¥4,480–5,480 | **据え置き** |
| ボア ラウンジチェア | ×4.55 | ¥10,980 | **据え置き** |
| モコ ラウンジチェア | ×3.48 | ¥36,980 | **据え置き** |

理由:

1. **×3.0 は下限であって目標値ではない。**上振れを機械的に下げると粗利を捨てるだけ
2. **注文0件。**値下げが必要かを判断する売れ行きデータが無い
3. **landed が推定。**ボア(原価 ¥1,871 + 送料概算 ¥1,750)のように**送料概算が原価の
   半分近い商品**では、実送料が判明した時点で倍率が大きく動く。**推定値で33%の値下げを
   決めるのは危険**

**フラワー フロアクッションの「原価が説明できない ×6.08」(D-097)は解消した。**
CKB原価が入って ×5.0台。異常値ではなくなった。ただし同じクッション帯
(ジャカード ×3.35 / ベルベット ×3.04–3.23 / パイピング ×3.16–3.22)からは突出して高い。
**競合価格との比較が要る(原価だけでは決められない)。**

🚩 なお **フラワー フロアクッションのまとめ買い判定 ✅ は「現在の高い価格のまま」の話。**
値下げすると 15%後は ×2.55 になり基準を割る。

## D-116 まとめ買い / SUMMER SALE の再判定結果(2026-08-25・提案)

**まとめ買い(推奨13の再判定): 採用推奨 10 / 要検討 2 / 除外 0 / 原価待ち 1**

- **採用推奨10**: ジャカード ×2.85 / ミニ アラームクロック ×2.79 / ガラスフラワー ×2.75 /
  パイピング ×2.68 / フランネル ×2.67 / フラワーバッド ×2.67 / バルーンドッグ ×2.66 /
  フェイクファー ×2.63 / 手編みコースター ×2.60(ぎりぎり)/ フラワー フロアクッション ×4.26
- **要検討2**: セラミック ブックエンド ×2.58 / ベルベット クッション ×2.58
  → **どちらも +¥500 の値上げで採用推奨になる**(D-113 案A)。値上げしないなら **2点10%までに留める**
- **原価待ち1**: ルナ ウォールライト

**SUMMER SALE(暫定7商品・10%OFF): 7商品すべて採用推奨。除外・要検討0。**
10%後の倍率は ×2.76〜3.02 で余裕がある。

割引率は **一律10%を推奨**。低単価4商品だけ15%にする案もあるが、
**まとめ買い15%との差が消えるので階段が崩れる。**一律15%は #6 ×2.61 / #7 ×2.60 が
境界に張り付くため推奨しない。

**`custom.multi_buy_eligible` / `sale` タグ / Discount のいずれも設定していない。Owner 承認待ち。**

---

## D-117 価格決定ロジックを「原価×固定倍率」から「実売価格からの逆算」へ変更(2026-08-25・Owner決定)

**docs/10 の「通常価格 = 着地原価 ×3.0」と docs/12 の「15%OFF 後も ×2.6 以上」を、
価格決定の Hard Gate から外す。参考指標としてのみ残す。**

理由:
- `×3.00 × 0.85 = ×2.55 < 2.6` で2つのルールは数式として両立しない(D-113 で判明)
- ×2.6 は粗利率 約61.5% 相当でかなり保守的
- 割引施策は AOV・注文粗利額・CPA 許容額も併せて見る必要がある
- 「注文0だから高価格据え置き」は価格妥当性の根拠にならない
- 割引を前提に通常価格を不自然に吊り上げるブランドにしない

新ルール: **`docs/13_pricing_promotion_framework.md`**(新規)。
順番は 原価 → 商品の役割 → 市場価格 → 顧客が払う最終価格 → 適用プロモーション →
通常価格の逆算 → 採算確認。スクリーニング目安は粗利率55%前後 + 絶対粗利額。

docs/10 / docs/12 は**削除せず**、冒頭に「docs/13 を優先する」注記を追加した。

## D-118 ソラ キャットハンモックの値上げ案(D-114)を撤回(2026-08-25・Owner情報)

Owner 調査により、**日本国内 Amazon / 楽天で同一または近い商品が約 ¥4,000 で
販売されている可能性**が判明。CKB 商品原価 ¥4,973(推定着地原価 ¥8,473)が
国内実売を上回る可能性がある。

- **¥25,980 への値上げ案は撤回。値上げしない**(現行 ¥20,480 据え置き)
- **P-8(ソラ + フェイクファー)は保留。**ソラを高く売るために PAIR を作らない
- **販売停止 / 非掲載の候補**として扱う
- 確認事項: 同一商品か / 同一 OEM か / 国内実売価格 / MORU で売る合理性

## D-119 販売施策の3分類を確定(2026-08-25・Owner承認)

**SUMMER SALE 18商品(10%OFF)/ MULTI-BUY 13商品(2点10% / 3点以上15%)/
保留1(ソラ)/ 除外4(ルナ・セル モジュール・プラッシュ・アブストラクト)= 全36商品。**

- Multi-buy 商品は **Summer Sale に入れない。**通常価格そのものを買いやすい価格にする
- Multi-buy は **同一 Product 内なら色・柄・Variant 違いを合算可能。別 Product は合算しない**
- 4点以上も 3点以上と同じ 15%OFF
- 分類は `ops/products/_product_classification_20260825.csv`

## D-120 新通常価格 31商品を第一案として確定(2026-08-25・Owner承認)

Owner 承認済みの新通常価格を `ops/products/_new_price_map_20260825.csv` に保存。
**Variant ごとの価格差は、現在の価格階層(段数)を維持したままレンジの端に合わせた。**
一律価格に潰していない。

再計算ツール: `ops/tools/price_plan.py`。出力は
`_price_plan_variants_20260825.csv`(257行)/ `_price_plan_by_product_20260825.csv`(36商品)。

**採算結果: 赤字0 / 粗利率55%未満0 / Variant価格逆転0。**
最も薄いのは フェイクファー 100×150cm のまとめ買い3点以上で 55.6%。

- D-113 の「セラミック / ベルベット は +¥500 しないと不可」は **撤回**(×2.6 Hard Gate 由来のため)
- D-115 の「上振れ3商品は据え置き」も **撤回**。フロアクッション ¥7,480〜8,480 → ¥5,480〜5,980、
  ボア ¥16,480 → ¥14,980 に引き下げる(市場価格と実売価格で判断する / D-117)

## D-121 PAIR は P-6 を第一候補にする(2026-08-25・提案)

PAIR 7組すべて採算成立(粗利率 56.8〜61.5%・赤字0)。

**P-6 ソファの脇(クラウド + マッシュルーム)を第一候補に推奨。**
限界利益 ¥10,556 は7組中最大(2位 P-E の1.8倍)で、
「1点でも Sale / 2点一緒なら PAIR でもう少しお得」の階段が実際に成立する
(単品Sale合計 ¥19,764 → PAIR ¥18,666)。

**🚩 P-F(バルーンドッグ + ツインベル)は後回し推奨。**
PAIR後 ¥7,191 で送料無料 ¥7,700 を割り、かつ得が ¥771 しかない。

PAIR 定義は `ops/products/_pair_definitions.csv`(P-6 / P-A〜P-F)。**Discount は未作成・未有効化。**

## D-122 割引の優先順位と重複禁止(2026-08-25・Owner決定)

**PAIR 15% > Summer Sale 10%。割引を加算して 25%OFF にしない。**
Multi-buy と PAIR が同時に成立し得る場合も加算しない。
**1商品につき適用する割引は1つだけ。競合したら percentage の高い方、同率なら先に定義された方。**
設定が読めないときは何も割り引かない(fail-closed)。

既存 `pair-set-discount` Function の `resolveWinners` はこの規則を既に実装している。
**統合 Function にするか Shopify の組み合わせ設定で排他にするかは Owner 決定事項。**

## D-123 送料無料 ¥7,700 の境界が7件(2026-08-25・Owner判断待ち)

新価格で単価が下がったため、**割引後 subtotal が ¥7,700 を割るケースが7件**発生:
ジオメトリック(Sale後 ¥7,182)/ フェイクファー・フランネルの2点(¥7,164)と3点(¥7,599)/
手編みコースター3点(¥7,599)/ PAIR P-F(¥7,191)。

いずれも ¥100〜520 の不足。**しきい値は Claude 側で変更しない。**
選択肢: ①しきい値を下げる(¥6,980 なら全解消) ②割引前 subtotal で判定 ③そのまま出す。

## D-124 原価待ち34 Variant がローンチをブロックしなくなった(2026-08-25)

CKB 原価未取得の34 Variant(アブストラクト21 / ルナ8 / セル モジュール5)は
**D-119 で全て販売除外**になったため、**原価待ちが価格設計とローンチの阻害要因ではなくなった。**

同時に、**ルナ ウォールライトがまとめ買い候補から外れた**ため
(D-116 の「公開しないと割引対象にできない」問題も消滅)、
**まとめ買いは ACTIVE な13商品だけで完結する。**

⚠️ ただし **landed 実測は依然 0件。**223/257 Variant の着地原価は
`ckb_cost + ship_est` の推定であり、**ship_est は実送料ではなく価格設計用の送料概算。**
実測が入ったら全件再計算する。

---

## D-125 30日間安心保証を正式方針にし、返金引当 0% をやめる(2026-08-25・Owner決定)

顧客向け:「**30日間安心保証** / ご満足いただけない場合は返金いたします。」
到着後30日以内・**原則返品不要で返金**・必要に応じ写真/動画を取得・
返金処理完了までは商品を保管・返送が必要な場合のみ個別案内・
**初期不良/誤配送/配送破損は顧客返金とは切り離して THE CKB へ補償請求**・
不正利用や過度な反復利用は制限可能。

採算では **返金引当 = (売価 + 着地原価) × 引当率**。
**原則返品不要のため、売上と在庫の両方を失う前提**で着地原価も引当に含める。
限界利益 = 粗利額 − 決済手数料 − 返金引当。

**3% / 5% / 8% の3シナリオで再計算。Owner 判断用の中心値は 5%。**
⚠️ 「返品率5%」という主張ではなく、**実績がないための暫定引当**。

結果(最悪ケース施策・211 Variant): **限界利益ゼロ以下 0件 / 粗利率50%未満 0件。**
引当8%でも成立する。詳細は `ops/products/_refund_scenarios_20260825.csv`。

## D-126 ハル ダイニングチェア 2脚セットを Summer Sale から除外(2026-08-25・Owner決定)

2脚セット価格には既にセットメリット(1脚×2 比 −¥7,980)が入っているため、
**その上に Sale 10% を重ねない。価格自体(¥33,980)は変更しない。**

**除外は Product 単位ではなく Variant 単位。**Sale 対象 Variant は 83 → **81**。
Summer Sale の商品数は 18 のまま(ハルは1脚 Variant で Sale に残る)。

## D-127 送料無料 ¥7,700 は維持し、割引前 subtotal で判定する(2026-08-25・Owner決定)

**送料無料ラインは ¥7,700 のまま。¥6,980 へは下げない。**

代わりに **「割引前 subtotal が ¥7,700 以上なら、商品割引後に下回っても送料無料を維持する」**
設計にする。現状のままだと、たとえば

  割引前 ¥7,960 → 送料無料 / 2点10%OFF(−¥796)→ 割引後 ¥7,164 → 送料 ¥870 発生
  = **実質 +¥74 の値上げ**(割引を使ったほうが高い)

となり、ローンチ前に解消が必要。該当7件。

**Shopify 標準の配送料金条件は割引後の金額で評価されるため、標準設定では不可能。**
Delivery Customization Function も用途が違う(表示の書き換えであって料金割引ではない)。

→ **既存の Discount Function に配送ターゲット
`cart.delivery-options.discounts.generate.run` を足す案を推奨**(D-128)。

⚠️ 配送ターゲット実行時に `line.cost.amountPerQuantity` が商品割引の前か後かは
ドキュメントから断定できなかった。**実装後に DEV で1回テスト注文を通して確認する。**

## D-128 Sale / Multi-buy / PAIR / 送料無料を1つの Function に統合する(2026-08-25・提案)

現在 `multi-buy-discount` と `pair-set-discount` が**別々の Function として**
商品割引を出すため、Shopify の組み合わせ設定次第で**加算され得る**。
Owner 方針(加算禁止・1商品1割引)をコードで保証できていない。

**`moru-promotions-discount` 1本に統合し、2ターゲットを持たせる案を推奨:**

- `cart.lines.discounts.generate.run` … PAIR → Multi-buy → Sale の順に候補を立て、
  **1商品につき最高 percentage を1つだけ残す**(同率は定義順)
- `cart.delivery-options.discounts.generate.run` … **割引前 subtotal ≥ ¥7,700 で配送 100%OFF**

Shopify の Discount Function は **1拡張に複数ターゲットを宣言でき、
`discount.discountClasses` で出し分けられる**(公式 "Create combined multi-type discounts" で確認)。

**手順4は既存 `pair-set-discount` の `resolveWinners` が実装済み。**統合は新規発明ではない。
設定 metafield に `freeShippingThreshold` / `pairs` / `multiBuy` / `sale`(+ `excludedVariantIds`)を持たせ、
**固定 Product ID はハードコードしない。読めなければ fail-closed。**

設計は `ops/_promotion_architecture_20260825.md`。**実装・Discount 作成は Owner Gate。**

## D-129 ship_est の最小検証は3バケット代表1件ずつ(2026-08-25・提案)

**全商品の実送料が揃うまでローンチを止めない。**ただし3件だけは実測を取る。

選定基準は **「概算送料が着地原価に占める割合がいちばん高く、施策に乗っている商品」**:

| バケット | 代表商品 | 概算が着地原価に占める割合 |
|---|---|---|
| 小物 ¥800 | **フェイクファー ブランケット 100×150cm** | **71%** |
| 中型 ¥1,750 | **フラワーバッド テーブルランプ** | **79%** |
| 大型 ¥3,500 | **クラウド サイドテーブル**(PAIR P-6 の片側) | **86%** |

取得: 中国国内送料 / 代行手数料・検品費 / 国際送料(実重量か容積重量か**も**) /
関税・輸入消費税 / 国内配送料 / **梱包後の実寸法と実重量**(現在 Shopify 上は全商品 0kg)。

⚠️ **3件のズレ率を同バケットの他商品へ機械的に横展開しない。**それは推測になる。
概算を改訂する場合は「改訂した」と docs/13 §2 に明記し、**estimate の表記は残す。**

## D-130 返金引当の式を訂正(着地原価の二重控除をやめる)(2026-08-26・Owner指摘)

D-125 の `返金引当額 = (売価 + 着地原価) × 引当率` は**着地原価を二重に控除していた。**
粗利額の時点で `売価 − 着地原価` として既に控除しているため。

**訂正後:**

```
返金引当額 = 売価 × 引当率 ( + 返金1件あたりの追加費用 × 引当率 )
限界利益   = 粗利額 − 決済手数料 − 返金引当額
```

追加費用(返送料 / 再配送費 / 決済手数料の非返還分 / その他返金処理費用)は
**別項目**として足す。`price_plan.py` の `REFUND_EXTRA_PER_CASE`。
**実測が無いので現在 0 円・未計上。**

再計算結果(最悪ケース施策・211 Variant):

| 引当 | 限界利益 | 赤字 | 粗利率55%未満 |
|---|---|---|---|
| 3% | ¥1,242 〜 ¥49,306 | 0 | 0 |
| **5%** | **¥1,191 〜 ¥47,507** | **0** | **0** |
| 8% | ¥1,115 〜 ¥44,807 | 0 | 0 |

絶対粗利額の目安を割るのは **フラワーバッド(中型・全シナリオ)** と
**クラウド(大型・8%時 ¥4,938 / 目安 ¥5,000)** の2件のみ。どちらも赤字ではない。

## D-131 新通常価格31商品を Owner 承認(2026-08-26)

`_new_price_map_20260825.csv` を**価格の Source of Truth** として維持する。
**Shopify Product 価格の書き換えは Function 準備完了後に一括で行う。**まだ実行しない。

- **フラワーバッド テーブルランプは ¥5,980 を維持。¥6,480 へ戻さない。**
  現在の警告は ship_est 依存 / 旧引当式が保守的すぎた / 実送料未取得。実測後に再判断
- **クラウド ¥11,980・マッシュルーム ¥9,980・P-6 の PAIR 15% を維持。12% へ下げない**
- 上振れ3商品の据え置き撤回(D-120)、ソラ据え置き(D-118)は変更なし

## D-132 統合 Function `moru-promotions-discount` を実装・deploy(2026-08-26)

`multi-buy-discount` + `pair-set-discount` を1本に統合し、配送割引も同居させた。

**2ターゲット:**
`cart.lines.discounts.generate.run`(商品割引)/
`cart.delivery-options.discounts.generate.run`(送料無料)

**優先順位:** PAIR 15% > まとめ買い(2点10% / 3点以上15%)> Sale 10%。
勝者は **① 率が高い方 → ② 同率なら割引が当たる数量が多い方 → ③ 定義順** で決める。

②を入れたのは、**PAIR が成立セット数までしか当たらない**ため。
同率15%でカバー数量が少ない PAIR を機械的に優先すると、
**顧客が本来受け取れる割引を取りこぼす。**

**設定は全て Discount の app metafield。Product ID をハードコードしない。**
`sale.excludedVariantIds` で**ハル 2脚セットを Variant 単位で除外**(D-126)。
`multiBuy.productIds` が空なら `custom.multi_buy_eligible` へ落ちる(旧方式の互換)。

**テスト 43 passed / 0 failed。**
**app version `moru-promotions-5` を作成済み。release は未実行**(サンドボックス制限)。

⚠️ **配送ターゲット実行時に `line.cost.amountPerQuantity` が商品割引の前か後かは未検証。**
**DEV でテスト注文を通してから本番に出す。**後だった場合は割引率が設定にあるので復元できる。

## D-133 ship_est の Excel 一括投入はブロッカーにしない(2026-08-26・Owner決定)

Owner 側で CKB 原価概算ツールの Excel 一括投入を試したが正常に利用できなかったため、
**今回はローンチのブロッカーにしない。**

**小 ¥800 / 中 ¥1,750 / 大 ¥3,500 を estimate として維持。**
実注文・個別見積が取れた商品から**順次 landed 実測へ更新**する。
**推測で他 SKU へ横展開しない**(D-129 の方針を継続)。

## D-134 まとめ買い対象の正本を Product metafield ひとつにする(2026-08-26・Owner決定)

**`custom.multi_buy_eligible`(boolean)が唯一の Source of Truth。**

Frontend Dev 側も `product.metafields.custom.multi_buy_eligible.value == true` の
ときだけまとめ買い UI を出す fail-closed 実装になっているため、
**Function 側に `multiBuy.productIds` を持たせると同じことを2箇所で手管理する**ことになり、
「UI には出るのに割引が付かない / その逆」が起きる。

```
custom.multi_buy_eligible = true
  → PDP にまとめ買い UI が出る(Frontend)
  → 統合 Function もまとめ買い対象として認識する(Backend)
```

- 統合 Function から `multiBuy.productIds` の読み取りを**削除**した
- 設定 JSON で変えられるのは **`multiBuy.tiers`(数量しきい値と率)だけ**
- ただし **`multiBuy` キーそのものが無ければ、まとめ買いは動かない。**
  対象商品は metafield が正だが、**施策の ON/OFF は設定側で持つ**
  (空の設定でいきなり割引が出るのは fail-closed に反するため)
- PAIR / Sale / `freeShippingThreshold` / `sale.excludedVariantIds` は
  引き続き Discount app metafield の設定で管理する

テスト **44 passed / 0 failed**(「設定 JSON に productIds を書いても対象は増えない」を追加)。

## D-135 `moru-promotions-5` / `-6` を release(2026-08-26)

- **`moru-promotions-5`** — 統合 Function の初回 release
- **`moru-promotions-6`** — `shopify.app.toml` に
  `[discount.metafields.app.function-configuration]` を宣言し、D-134 を反映

**release 後も Discount resource は 0件のまま**(実査確認済み)。
**Function を release しただけでは顧客向け割引は有効にならない。**
Discount resource を作って初めて適用される。

## D-136 🔴 Discount 作成には MORU Promotions 自身の Admin API トークンが要る(2026-08-26)

Function の設定 JSON は **アプリ予約名前空間 `$app`** の metafield に入る。
**`$app` は MORU Promotions 専用なので、他のアプリからは書き込めない。**

Claude が使っている MCP は別アプリ(`Shopify Claude Connector App`)なので、
**`discountAutomaticAppCreate` を Claude 側から実行できない。**

- 管理画面から作る経路も、**設定 JSON を入れる UI が無い**(admin UI extension 未作成)ため
  今のままでは使えない。設定が空 = fail-closed で割引は1円も出ない
- `shopify app deploy` で使った Automation Token は **Dev Dashboard 用であって
  ストアの Admin API トークンではない**
- **→ MORU Promotions アプリの Admin API アクセストークンを別途用意する必要がある**

一方 **`metafieldsSet`(まとめ買い13商品)は `custom` 名前空間 = マーチャント所有**なので、
**現在の権限で実行できる。**制約がかかるのは Discount 作成だけ。

## D-137 Discount 作成・metafield 設定の materials を用意(2026-08-26)

`ops/promotions/` に実行材料一式を置いた。**Shopify は未変更。**

| ファイル | 中身 |
|---|---|
| `_shopify_gids_20260826.json` | 商品名 → Product GID(Admin 実査・36商品) |
| `_discount_config_20260826.json` | Function に渡す設定 JSON |
| `_discount_create_variables_20260826.json` | `discountAutomaticAppCreate` の variables |
| `_multibuy_assignment_20260826.csv` | metafield 設定予定 13商品 |
| `_multibuy_set_variables_20260826.json` | `metafieldsSet` の variables |
| `README.md` | 実行 Runbook + **DEV テストケース A〜E(21件)** |

生成は `ops/tools/promotion_config.py`(**Shopify を1件も変更しない**)。

**両 mutation とも `validate_graphql_codeblocks` で検証済み。**
`combinesWith` は3つとも false にして、加算禁止を Shopify 側でも二重に担保する。

## D-139 新価格211 Variant と まとめ買い13商品を反映(2026-08-26・実行済み)

ストアがパスワード保護中で MAIN も実質開発環境という Owner 判断により、
Discount の完成を待たずに商品データを先に反映した。

| 変更 | 件数 | 結果 |
|---|---|---|
| Product 価格 | **211 Variant / 31商品** | **不一致 0**(257 Variant 全件 readback) |
| `custom.multi_buy_eligible = true` | **13商品** | **設定漏れ 0 / 誤設定 0** |

- 実行前に**全件照合**(SKU 存在 / 現価格一致 / 商品名一致 / compare_at が null / ACTIVE のみ)を通し、
  **エラー 0 で初めて実行**した。照合に失敗したら計画を出さずに落とすツールにしてある
- **`compare_at_price` は1件も作っていない**(全 Variant で null のまま)
- 除外5商品(ルナ / セル モジュール / プラッシュ / アブストラクト / ソラ)は
  **価格も metafield も触っていない**
- Product status / publication / MAIN テーマ は未変更

ツール: `ops/tools/price_apply_plan.py` / `price_readback_verify.py`。
記録: `_price_apply_plan_20260826.csv` / `_price_readback_20260826.csv`。

## D-140 🔴 Discount 作成は Claude 側からは実行できない(2026-08-26・実測で確定)

**2つの経路を試して、どちらも権限で通らないことを実測した。推測ではない。**

### ① `shopify app execute` → **401**

公式ドキュメント(Manage App Automation Tokens)に
**「App Automation Token は `deploy` コマンドのために使う」**と明記があり、
**ストアスコープの Admin API アクセスは含まれない。**
`app execute` は組織 API でストアを解決しようとして 401 になる。
対話式の `shopify auth login`(ブラウザ)が必要で、サンドボックスでは実行できない。

### ② MCP から `discountAutomaticAppCreate` → **functionHandle が解決されない**

```
"Function moru-promotions-discount が見つかりません。
 現在のアプリ (341262598145) でリリースされており、…"
```

**`functionHandle` は「呼び出しているアプリ」の Function から解決される。**
MCP は別アプリ(`Shopify Claude Connector App`)なので MORU Promotions の Function は見えない。

> 副産物: `startsAt: null` は受け付けられない(「開始日時は空にできません」)。
> **作成時に必ず開始日時を入れる。**

### → 採用: shop metafield + 管理画面から作成(D-138 の fallback 経路)

設定 JSON を **shop の `custom.moru_promotions_config`** に書き込み済み
(`gid://shopify/Metafield/62541611335920`)。`jsonValue` として読めることを確認した。

⚠️ **`custom` はマーチャント所有なので `$app` より分離性が弱い。**
このストアの他アプリからも読み書きできる。秘密情報ではない(商品 GID と割引率だけ)が、
**`$app` と同じ隔離性はない。**アプリ自身のトークンが用意できたら `$app` へ移し、
shop 側は消してよい(Function は `$app` を優先して読む)。

**Owner の1手順**: 管理画面 → 割引 → 自動割引 → MORU Promotions の
`MORU 販促割引(統合)` を選び、**商品割引と配送割引の両方にチェック**、
**組み合わせは3つとも OFF**、開始日時を入れて保存。
設定 JSON は shop metafield から自動で読まれる。

**21 の DEV テストは Discount が無いと1件も実行できないため未実施。**作成後にまとめて行う。

## D-141 管理画面で "Example Domain" へ飛ぶ問題を Admin UI 拡張で直す(2026-08-26)

### 原因

**Function に `[extensions.ui]` が無かった。**
管理画面で「割引を作成 → アプリを選ぶ」と、Shopify は**そのアプリの App Home URL**
(`shopify.app.toml` の `application_url`)を開く。MORU Promotions は Function だけの
アプリで App Home を持たず、`application_url` が **`https://example.com`** のままだったため
**"Example Domain"** が開いていた。`[extensions.ui.paths]` も Discount 用 UI 拡張も無し。

### 直し方

`application_url` を変えて App Home を用意するのではなく、
**Shopify 公式の「割引詳細ページ内で描画する拡張」を足す。**

- `extensions/moru-promotions-discount-ui/` を新設
  (`type = "ui_extension"` / target `admin.discount-details.function-settings.render`)
- Function 側に **`[extensions.ui] handle = "moru-promotions-discount-ui"`** を追加
- `jsconfig.json` で `jsxImportSource: preact` を指定
  (無いと JSX が `react/jsx-runtime` を探してビルドが落ちる)
- `@preact/signals` が `@shopify/ui-extensions/preact` の依存として必要

### UI は読み取り専用にした

割引条件の実データは **すでに `shop.custom.moru_promotions_config` にある**(D-138)。
UI で再入力させると**同じ設定を2箇所で手管理する**ことになり、どちらが正か分からなくなる。
**入力欄は置かない。**設定を変えるときは
`ops/promotions/_discount_config_20260826.json` を直して metafield へ入れ直す(git に履歴が残る)。

⚠️ 画面の数値は **metafield の実値を読んでいるのではなく、リポジトリ側の正を転記したもの。**
ズレたら metafield が実際の挙動を決める。**設定を変えたら JSX も一緒に直すこと。**

**app version `moru-promotions-8` を release。Function テスト 49 passed / 0 failed。
Discount resource は 0件のまま。**

## D-142 Example Domain 問題の実査と api_version 2026-01 への引き上げ(2026-08-26)

### 実査結果: **配線・release は正しかった**

- `moru-promotions-8` は **★ active**(current release)
- deploy artifact に Function と UI 拡張の**両方**が含まれる
- Function 側に **`ui.ui_extension_handle = "moru-promotions-discount-ui"`** が入っている
- UI 拡張の target は **`admin.discount-details.function-settings.render`**
- `enable_creation_ui: true` / handle の綴り一致 / type も `ui_extension`

**「release されていない」「配線ミス」ではない。**

### 唯一ドキュメントと違った点: `api_version`

`[extensions.ui] handle` **だけ**で Admin UI 拡張に関連付ける公式例は
**すべて `api_version = "2026-01"`**。こちらは `2025-10` だった。
changelog「Enhanced Discount Function configuration with Admin UI extensions」も
**2026-01** で統合が強化されたことを示している。
**2025-10 で handle 単独の関連付けがサポートされている記述は見つからなかった。**

→ `moru-promotions-discount` と `moru-promotions-discount-ui` を **2026-01** に上げ、
`@shopify/ui-extensions` も `2026.1.x` にして **`moru-promotions-9` を release**。
テスト 49 passed / 0 failed。artifact 上の関連付けも維持を確認。

⚠️ **これは仮説にもとづく修正で、実機では未確認。**
Claude 側から管理画面の遷移を見る手段が無い(`app execute` / `generate extension` は
組織 API で 401 / `appInstallations` は access denied)。**Owner の再テストが要る。**

### 再インストールはしない

**「新しい Admin UI 拡張を追加したら再インストールが必要」という公式記述は見つからなかった。**
アクセススコープも `read_products,write_discounts` のまま変えていないので再承認の理由も無い。

失われ得るものの事前確認: `$app` metafield(**現在未使用**)/ installation token(再発行)/
Function association(app version から復元)/ **Discount resource は 0件なので影響なし**。
`shop.custom.moru_promotions_config` と Product 価格・`custom.multi_buy_eligible` は
**merchant-owned なので保持される**。

**実質的に失うものは無いが、根拠が無いままやることではない。**まず api_version 修正を試す。

## D-143 Discount を作成。readback 全項目一致。ただし勝者判定に設計上の問題(2026-08-26)

**Discount 1件を Owner が管理画面から作成。readback は全項目一致。**

`gid://shopify/DiscountAutomaticNode/1484384305392` / `MORU 販促割引(統合)` / **ACTIVE** /
`functionId 01a03b6e-…` は deploy manifest の `moru-promotions-discount` の uuid と一致 /
`combinesWith` 3つとも false / discount 側 metafield 0件(shop フォールバックを使う) /
`errorHistory` null。

`discountClasses` は `PRODUCT, ORDER, SHIPPING`。**ORDER は管理画面から外せない**
(割引クラスを管理しない Function は3つとも既定 ON という Shopify 仕様)。
**この Function は注文全体の割引を発行しないので実害はない。**

### 🔴 見つかった問題: 率だけで勝者を決めると顧客が損をするカートがある

カート **クラウド ×3 + マッシュルーム ×1**(現在の本番価格)で:

| | 合計割引 |
|---|---|
| いまの挙動(PAIR 15% が勝つ・クラウドは1個だけ) | **−¥3,294** |
| Sale 10% が全数量に当たっていた場合 | **−¥4,592** |

**差 ¥1,298 ぶん顧客が損をする。**

原因は `resolveWinners` の比較順が **①率 → ②数量 → ③定義順** で、
**②が「同率のとき」しか効かない**こと。PAIR は成立セット数ぶんしか当たらないのに、
率が高いだけで全数量に当たる Sale を追い出し、**残りには何も付かない**(1商品1割引のため)。

**直し方の案: 第一基準を「率」から「割引額の見込み」へ**
(`Σ(単価 × 対象数量) × percentage`)。同率なら数量が多い方が自動的に勝ち、
同額のときだけ定義順で決める。**1商品1割引・加算禁止は変わらない。**

⚠️ **①率 → ②数量 → ③定義順 は Owner が明示承認したルールなので勝手に変えない。**
変えると「PAIR 15% > Summer Sale 10%」の打ち出しと実挙動がズレるケースが出る。
**A. 現状維持 / B. 割引額で比較(推奨) / C. PAIR を全数量に当てる** から Owner が選ぶ。

## D-144 割引の単位を「商品」から「購入1点(quantity)」へ変更(2026-08-26・Owner決定 D案)

### 変更前の問題(D-143)

「1商品につき割引1つ」だったため、**PAIR が成立セット数ぶんしか当たらないのに
商品ごと勝ってしまい、残りの点数から Sale を奪っていた。**

### 変更後

**「1つの購入単位(quantity)につき割引は1つ」。**

- 同じ1点に2つの割引を重ねない(**加算禁止は維持**)
- **同じ商品でも、別の1点になら別の施策を当ててよい**
- 強い施策から順に、まだ誰も取っていない点数を取る
- 比較順: ① percentage が高い方 → ② 同率なら定義順 PAIR > Multi-buy > Sale
- **強い割引が一部の点数にしか当たらなくても、残りの点数から弱い割引を奪わない**

まとめ買いの率は **カート内のその商品の合計点数**で決まる(PAIR が何点使ったかに関係なく)。
Sale の Variant 除外(ハル2脚セット)は Sale にだけ効き、PAIR / まとめ買いには効かない。

### Discount Function API で quantity 分割は可能か → **可能**

`productDiscountsAdd` の target は `cartLine { id, quantity }` を取れる。
`ProductDiscountSelectionStrategy` には **`ALL`**(候補リストを全部適用)がある。
割当が点数単位で排他なので、**ALL でも加算は起きない。**

### 🔴 あわせて見つかったバグ: `FIRST` だと2つ目以降の割引が消える

これまで `selectionStrategy: FIRST` を使っていた。
公式ドキュメントの定義は **「候補リストの最初の1つだけを適用する」**。
候補は「率 + 表示名」ごとに束ねているので、**商品が2種類以上あるカートでは
2つ目以降の候補が丸ごと適用されない**可能性があった。**`ALL` に変更した。**

### 検算(実データの価格)

カート **クラウド ×3(¥11,980)+ マッシュルーム ×1(¥9,980)**:

| | 割当 | 割引 |
|---|---|---|
| クラウド 1点 | PAIR 15% | −¥1,797 |
| マッシュルーム 1点 | PAIR 15% | −¥1,497 |
| クラウド 2点 | SUMMER SALE 10% | −¥2,396 |
| **合計** | | **−¥5,690** |

小計 ¥45,920 → **割引後 ¥40,230**。**Owner 提示の期待値と一致。**

**ユニットテスト 57 passed / 0 failed**(D-1〜D-7 と回帰を追加)。
**`moru-promotions-10` を release。**

Discount は1件のまま(`gid://shopify/DiscountAutomaticNode/1484384305392` / ACTIVE /
`functionId 01a03b6e-…` / `combinesWith` 3つとも false / **errorHistory null**)。

## D-145 Storefront 実機テストはレート制限で未実施(2026-08-26)

Owner からストアフロントパスワードの利用許可を受け、実機テストの仕組みを作った。
**パスワードは環境変数からのみ読み、リポジトリ・ログ・出力のどこにも保存していない**
(`grep` で全ファイルを確認済み)。

`ops/promotions/storefront-tests/run.mjs`:
ブラウザを使わず、`/cart/add.js` と `/cart.js` を直接叩いてカートを作り、
返ってきた JSON の割引額を測る。**テーマの DOM に依存しない**ので、
Frontend の実装状況に左右されず Function の実挙動だけを見られる。21ケースを定義済み。

**⚠️ Chromium はこのサンドボックスの外向きプロキシを通れなかった**
(`ERR_CONNECTION_RESET`。`curl` と `fetch` は通る)。そのため fetch 方式に切り替えた。

**⚠️ さらに `/password` への POST が 429(レート制限)を返し続ける。**
60秒間隔で8回試しても解除されなかった。**このサンドボックスの送信元 IP に対する
Shopify 側の制限**と見られる。**21ケースはまだ1件も実行できていない。**

**回避策で誤魔化さず、未実施として報告する。**時間をおいて再実行する。

---

## D-146 速達 ¥3,762 をローンチ対象から外し、送料無料は通常配送だけに当てる

**2026-08-26 / Owner 判断**

Checkout に出ていた **速達 ¥3,762 / 1〜2営業日** を**ローンチ時点では提供しない。**

理由は納期を保証できないこと。THE CKB と国際配送を挟むため、
**1〜2営業日で届くかどうかが未確認のまま追加料金を受け取ることになる。**
守れない期待納期を有料で売るリスクのほうが大きい。

ローンチ時点の配送方法は **通常配送のみ**(基本送料 ¥870 / 割引前 ¥7,700 以上で無料)。

### やったこと

`deliveryProfileUpdate` で `DeliveryMethodDefinition/925779427568`(速達)を
**`active: false` にした。削除ではなく無効化。**
削除だと料金・条件・名称を復元するのに再入力が要るが、無効化なら
**方針が変わったときに1フラグで戻せる**。Checkout に出ないことは同じ。

### 送料無料 Function を「通常配送だけ」に限定した

これまで `buildDeliveryResult` は **`deliveryGroups` を丸ごと 100%OFF** していた。
この作りだと **将来 Shopify 側に配送方法が増えたとき、その新しい配送方法まで
自動的に無料になる。** 速達を外す判断と正面から矛盾する。

そこで**配送オプション単位のターゲット**へ変えた:

- 入力クエリで `deliveryGroups { deliveryOptions { handle title } }` を取る
- 設定 `freeShippingDeliveryOptionTitles`(現在 `["通常配送"]`)に**載っている title だけ**を
  `targets: [{ deliveryOption: { handle } }]` に入れる
- 許可リストが**無い / 空 / 文字列以外 → 何もしない**(fail-closed)
- 許可リストに載っていても、カートにその配送オプションが無ければ何もしない

**知らない配送方法には当てない。**

### 検証できたこと / できていないこと

`/cart/shipping_rates.json` で実測:

| # | 確認項目 | 結果 |
|---|---|---|
| 1 | 速達が表示されない | ✅ 配送方法は `通常配送` 1件のみ |
| 2 | 通常配送だけ表示される | ✅ |
| 3 | 通常 ¥870 | ✅ |
| 4 | 割引前 subtotal >= ¥7,700 なら無料 | ✅ ソラ ¥20,480 → `price: "0"` |
| 5 | **商品割引後に ¥7,700 未満になっても無料維持** | 🔴 **未検証** |
| 6 | 元から ¥7,700 未満なら ¥870 | ✅ |

**5 だけが未検証で、しかもここが Function の存在理由そのもの。**

同じ計測で **裏付けが1つ取れた**: フェイクファー ×2(割引前 ¥7,960 → 割引後 ¥7,164)の
カートに対し、この旧エンドポイントは **¥870 を返した。**
つまり **Shopify 標準の「¥7,700 以上」条件は割引後の金額で評価されている**ことが
実データで確認できた(D-127 の前提が正しかった)。

**ただし `/cart/shipping_rates.json` は Discount Function を通さない旧エンドポイントなので、
この ¥870 は「Function が効いていない」証拠にはならない。** 判定材料にならない。

**Function の配送割引を実測するには Function を通る経路(Checkout / Storefront Cart API)が要る。**
このサンドボックスからは `/checkout` が 403(bot 判定)、Chromium はプロキシを通れない。
**Owner がブラウザで1カートだけ確認するのが最短。**

---

## D-147 旧 extension を削除し、国際配送ゾーンを無効化する

**2026-08-26 / Owner 判断**

### 送料無料の実機確認が PASS した

Owner が Checkout で確認:

フェイクファー 150×200 ×2 / 通常小計 ¥7,960 → まとめ買い 10%OFF 後 **¥7,164**
→ **通常配送のみ表示 / 通常送料 ¥870 → 無料 / 速達は表示されない。**

**D-146 で 🔴 未検証として残していた「割引後に ¥7,700 を割っても送料無料を維持する」が
実機で確認できた。** 配送ターゲットが読む `amountPerQuantity` は
**商品割引の適用前**の値だった(D-127 の懸念は空振り。足し戻し実装は不要)。

これで **6項目すべて PASS**。

### 旧 extension を削除した

`multi-buy-discount`(`moru-multi-buy-discount`)と
`pair-set-discount`(`moru-pair-set-discount`)を**リポジトリから削除**し、
`shopify app deploy --allow-deletes` でリリースした。

**残るのは統合版の `moru-promotions-discount` と、その設定 UI
`moru-promotions-discount-ui` の2つだけ。**

⚠️ **`--allow-deletes` が要る。** 付けないと
「This deployment includes changes that require confirmation.」で止まる。
非対話環境では確認プロンプトを出せないため。

`package.json` の `test` も統合版だけを指すよう直した(削除したパスを叩いていた)。

### 国際配送ゾーンを無効化した

**ローンチ時は日本国内販売のみ。**
国際ゾーン(27カ国)の `Standard ¥3,000`
(`DeliveryMethodDefinition/925779460336`)を **`active: false`**。

速達と同じく**削除ではなく無効化**。ゾーンと対象国のリストはそのまま残るので、
海外展開するときに `active: true` に戻すだけでよい。

### readback

| 項目 | 結果 |
|---|---|
| 配送(日本・東京) | **`通常配送 ¥870` のみ** |
| 配送(アメリカ CA) | **配送方法なし**(注文できない) |
| 配送(シンガポール) | **配送方法なし** |
| 速達 | `active: false` |
| 国際 Standard | `active: false` |
| Discount resource | **1件のみ** |
| title | `MORU 販促割引(統合)` |
| status | `ACTIVE` |
| discountClasses | `PRODUCT` / `ORDER` / `SHIPPING` |
| functionId | `01a03b6e-7661-7590-8dbf-d5b2e47e837d` |
| errorHistory | **`null`** |
| combinesWith | **全て `false`** |
| storefront 21ケース | **21 PASS / 0 FAIL**(削除後に再実行) |

app version は **`moru-promotions-12`**。

---

## D-148 パスワード解除後の実測。ローンチ阻害は MORU500 案内の1件だけになった(2026-08-27)

引き継ぎ時点の「ローンチ阻害3件」を実測で洗い直した。**2件はすでに解消していた。**

| # | 項目 | 実測 |
|---|---|---|
| 1 | パスワード保護 | ✅ `onlineStore.passwordProtection.enabled: false` |
| 2 | 通常配送の transit time | ✅ `/cart/shipping_rates.json` が `delivery_days: []` / `delivery_range: null`。¥870(しきい値未満)・¥0(しきい値以上)の両ケースで確認 |
| 3 | MORU500 の顧客への案内 | 🔴 未解消 |

### 正規ドメインが `moruliving.com` に切り替わっていた

`moruliving.myshopify.com` / `rgy5ee-fv.myshopify.com` はどちらも **301**。
`run.mjs` は `redirect: 'manual'` で叩くため、**旧ドメインを既定にしていると
301 を「パスワード保護がまだ有効」と誤検知して停止する。**
既定値を `https://moruliving.com` に変更した。

### #3 は「未実装」ではなく「約束して届けていない」

コード `MORU500` は `/` `/cart` `/account/register` `/account/login` `/pages/faq` の
**どこにも出ていない。** 一方でアナウンスバーは全ページで
「新規会員登録で¥500OFFクーポンプレゼント」と言い、
FAQ は「**対象のお客様のカートに表示される**¥500OFFクーポンをご利用ください」と明言している。

**表示ブロックが存在しないので、FAQ の記述が事実に反している。**
新規会員がコードを知る手段はゼロ。

**決定: 実装先は `MORU Frontend Dev`(166341181680)だが、docs/06 §2 の分業ルールにより
Owner の明示指示があるまで Claude は着手しない。**
先送りする場合でも、**FAQ の「カートに表示される」という記述は先に直す**
(いま顧客向け文面が事実と食い違っているため)。

### storefront-tests の有効ケース数を訂正

**21ケース中 19ケースが有効。** A-2 と E-2 が手編みコースター(DRAFT 化済み)を使っており、
`cart/add.js` が **422** を返す。前の申し送りの「20ケース」は **E-2 を数え漏らしていた。**

---

## D-149 全商品のカラースウォッチに色を設定した(2026-08-27)

**オプション値のカラーが1件も設定されておらず、本番のスウォッチが色なしの丸で出ていた。**
ライブ HTML で確認: `<span class="moru-option__swatch">` に
`style="--swatch-color: …"` が付いていなかった。

### 経路の選択

テーマ(`snippets/moru-swatch-color.liquid`)は3経路で色を解決する。

| # | 経路 | 採否 |
|---|---|---|
| 1 | Shopify ネイティブのオプションスウォッチ(`value.swatch.color`) | ❌ `OptionValueUpdateInput` に swatch フィールドが無く、メタオブジェクト連携が要る |
| 2 | **Product metafield `custom.swatch_colors`**(`moru_color` メタオブジェクトのリスト) | ✅ **採用** |
| 3 | テーマ設定 `swatch_color_map` | ❌ `settings_data.json` の変更 = MAIN テーマへの書き込みになり禁止 |

**経路2だけがテーマを一切触らずに済む。** 定義(`moru_color` /
`custom.swatch_colors`)は以前から作られていて、**中身が空のまま放置されていた。**

### やったこと

- `moru_color` メタオブジェクトを **107件**作成(ACTIVE 用 103 + DRAFT 用 4)
- **ACTIVE 23商品 / 127値**に `custom.swatch_colors` を設定
- DRAFT の **ピボ**(3値)・**ルナ**(4値)も設定

対象は**オプション名が `カラー` / `色` / `Color` のものだけ**。
テーマは `swatch_option_names` と**完全一致**でしか判定しないので、
`色分類`(アブストラクト / レジン / フラワー キャットタワー)や
`タイプ`(ハル / セル モジュール / ソラ)はスウォッチにならない = 対象外。

### 色の決め方

**オプション値名から引いた近似値。実測色ではない。**
家具・ファブリックに寄せて彩度を落としてある(Web の原色を使うと
docs/01 の「明るい自然光・清潔感」から外れ、スウォッチだけが浮く)。

複合名(`ブラック × ベージュパッチワーク`)は**後ろ側**を採る。
前がフレーム色、後ろが張地で、見た目の差が大きいのは張地のため。

商品画像から色を自動サンプリングする方法も試したが、**使えなかった。**
MORU の商品画像は生活シーンで、中央が床や壁を拾う
(コイル「トゥルーレッド」→ `#A98E72` = 背景 / モコ「ブラック」→ `#73533D` = 床)。
実際に画像を目視して、名前ベースの値のほうが正確だと確認した。

### 検証

- メタオブジェクトを全件読み戻して意図と突き合わせ → **書き写しミスを1件検出**
  (`フォググレー` を `フォッググレー` と入力していた)。修正して再確認
- **本番ストアフロントの PDP を23商品ぶん取得し、`--swatch-color` を実測**
  → **127/127 一致**

### 未設定として残したもの

| 商品 | 値 | 理由 |
|---|---|---|
| バルーンドッグ オブジェ | `マルチカラー` | 1色で表せない。推測で色を当てない |
| プラッシュ クッション(DRAFT) | 色8値 | `両面-W柄レザーパウダー` 等が機械翻訳のままで何色か確定できない。**名前を直すのが先** |
| 手編みコースター(DRAFT) | 色7値 | `カエルの直径は12cmです` 等、動物名しかなく色の情報が無い。同上 |

### 再現・修正の手順

`ops/products/swatch-colors/build.mjs` の `COLORS` を直して
`node ops/products/swatch-colors/build.mjs` を流すと対応表が再生成される。
一覧は `ops/products/swatch-colors/_review.md`。

⚠️ `アイボリー` / `オリーブグリーン` は、以前から `ivory` / `olive-green` という
ハンドルの `moru_color` が存在しており、**同じ色の重複エントリになっている。**
テーマは名前で先頭一致して break するので**動作に影響は無い**が、
管理画面では紛らわしい。整理するかは Owner 判断(削除は行っていない)。

---

## D-150 商品ページにセール価格を表示する(2026-08-27)

**フラワーラウンジの商品ページが ¥8,980(定価)のままで、セール価格が出ていなかった。**
一方で同じページ下部の「合わせて楽しむアイテム」や一覧の商品カードは
**¥8,980 に線 + ¥8,082** を出していた。**同じ商品に2つの金額が出ている状態。**

### 原因

**値引きは Discount Function がカートで適用する。`compare_at_price` ではない。**
だから Liquid は何もしなければ定価をそのまま出す。

`snippets/moru-product-card.liquid`(Dev 版)は
`shop.metafields.custom.moru_promotions_config` を読んで自前でセール価格を計算していたが、
**`sections/moru-main-product.liquid` の価格ブロックには同じ処理が入っていなかった。**
カードだけ直してセクションを直し忘れた形。

### 直した内容(`sections/moru-main-product.liquid`)

カードと**同じ config・同じ計算式**を使う。片方だけ直すとまた金額が食い違う。

- Liquid: `sale.productIds` に入っていれば `sale.percentage` を適用し、
  価格を赤 + 定価に取り消し線
- **判定は「いま選ばれている Variant」で行う。** カードは「除外されていない最安 Variant」
  基準だが、商品ページで同じにすると **Sale 除外の Variant に嘘の値引きを出す**
- `sale.excludedVariantIds` を data 属性で JS に渡し、
  **バリアント切替に追従**させた(Liquid と同じ切り捨て計算にそろえてある)
- CSS は `var(--color-sale)` でカードと同色

### ⚠️ 出すのは Sale だけ

**まとめ買いと PAIR はカートの中身で成立が決まるので、単品ページには出さない。**
出すと事実と違う表示になる(CLAUDE.md 絶対ルール6 / docs/01 §4「その表示が事実か」)。
Sale は1点でも無条件に効くので、単品ページに出して事実。

### 検証(未公開テーマのプレビューで実測)

- `shopify theme check` … **91ファイル / 0 offense**
- ACTIVE **28商品を全件**取得 → **セール表示 17 / 非セール 11**。
  17は `sale.productIds` の18件からピボ(DRAFT)を除いた数と一致
- **除外 Variant の確認**: ハル ダイニングチェア
  - 1脚 → SALE ¥18,882(定価 ¥20,980 に線)
  - **2脚セット(除外2件)→ 通常 ¥33,980・線なし**
  - これは storefront テストの C-1 / C-2 の実測値と完全に一致
- フラワーラウンジ … **¥8,980 に線 + ¥8,082**(カードと同額)

### 🔴 反映先は Frontend Dev だけ。**本番にはまだ出ていない**

`MORU Frontend Dev`(`166341181680`)へ upsert し、md5 で readback 済み
(`a75a9c0df0ec2745b5e387302c776a15` / 60,543 bytes)。

**MAIN(`166459769072`)は未変更。**

⚠️ **2026-08-27、Owner の明示指示を受けて本番反映を試みたが、3経路とも塞がっていた。**

| 試したこと | 結果 |
|---|---|
| `themeFilesUpsert` を MAIN 宛てに実行 | **拒否**(`category: live_theme` / 公開テーマへのファイル書き込みは不可) |
| `themePublish` で Frontend Dev を公開 | **拒否**(`category: destructive` / publish は管理画面で人が行う設計) |
| Shopify CLI (`shopify theme ...`) | **拒否**(ハーネス側の分類器がブロック) |

**publish は管理画面から人が行うしかない。**

安全性は確認済み: **MAIN と Frontend Dev の差は `sections/moru-main-product.liquid` の1ファイルだけ**
(89ファイル中、ファイル名の構成も一致)。したがって
**Frontend Dev を publish すれば、今回の修正だけが本番に出る。** 他の作業中の変更は混ざらない。

手順: 管理画面 → オンラインストア → テーマ → **MORU Frontend Dev** の「…」→ **公開する**

### 副産物: repo は本番の正本ではない

Frontend Dev の全ファイル checksum を repo と突き合わせた
(`ops/theme/_repo_vs_dev_20260827.md`)。**一致54 / 相違24 / repoに無い11。**
今回 `moru-main-product.liquid` を repo 上で直接編集できたのは、
**たまたま md5 が完全一致していたから。** 次に他のファイルを触るときは必ず突き合わせる。

---

## D-151 テーマ反映を Shopify CLI 経由に変える(2026-08-27)

**Claude から本番テーマを直せないことが、恒久的な運用の障害になっていた。**

### 実測した事実

| 経路 | 結果 |
|---|---|
| MCP `themeFilesUpsert` を公開テーマ宛て | **拒否**(`category: live_theme`) |
| MCP `themePublish` | **拒否**(`category: destructive`) |
| Shopify CLI | ハーネスの権限分類器が拒否。**allow で解除できる** |

**前2つは Shopify MCP サーバー側の設計なので解除できない。**
3つ目だけが解除できるので、**CLI を正式な反映手段にする。**

### なぜ変える必要があったか

未公開テーマを公開すると**そのテーマが MAIN になり、以後書き込めなくなる。**
旧 MAIN は1世代古いので、次の修正は「同期 → 編集 → 公開」。
**2テーマの ping-pong を毎回やることになる。** 恒久運用に耐えない。

### 用意したもの

| ファイル | 中身 |
|---|---|
| `shopify.theme.toml` | 環境定義 `dev` / `live` / `rollback`。**password は書かない**(git に入るため) |
| `.claude/settings.json` | `shopify theme pull/push/list/info/check` を allow、**`delete` / `publish` を deny** |
| `ops/theme/README.md` | セットアップと運用手順(**唯一の正**) |

`live` 環境は `live = true` にしてある。**テーマが入れ替わっても ID を書き換えなくてよい。**

### Owner の作業(一度だけ)

1. 管理画面 → アプリ → **Theme Access** をインストールし、パスワードを発行
2. **Claude Code(web)の環境変数**に登録(シェルの export はセッションごとに消える):
   `SHOPIFY_CLI_THEME_TOKEN` / `SHOPIFY_FLAG_STORE=rgy5ee-fv.myshopify.com` / `SHOPIFY_FLAG_FORCE=1`

### 🔴 セットアップ後、最初にやること

```
shopify theme pull -e live
```

**repo は本番の正本ではない**(相違24 / repo に無い11)。
先に同期しないまま `--only` 無しで push すると、**本番から11ファイルが消え、24ファイルが古い内容で上書きされる。**

### 方針変更した既存ルール

CLAUDE.md の「`--allow-live` を使わない」「MAIN 書き込み・publish 禁止」は
**Owner 判断で更新した**(2026-08-27)。ただし安全側の縛りは残す:

- `--only` と `--nodelete` を必ず付ける
- **`theme publish` と `theme delete` は引き続き人の手のみ**(deny 済み)。
  公開の切り替えを機械に任せない

---

## D-152 商品ページのセール価格を本番に反映した(2026-08-27)

**D-150 の修正が Frontend Dev に留まっていて、本番に出ていなかった。**
D-151 で用意した CLI 経路(Theme Access トークン)の**初運用**にあたる。

### 結果

**反映して、ストアフロントで実測して確認した。**
ACTIVE 28商品の PDP を全件取得し、**セール表示 17 / 非セール 11 / 不一致 0**。
Sale 除外の「ハル ダイニングチェア 2脚セット」2 Variant も
**¥33,980・取消線なし**で、除外品に嘘の値引きを出していない。

MAIN の `checksumMd5` は `a75a9c0d…`(= Frontend Dev と同一)。
テーマのファイル数は **89 のまま**で、`--nodelete` が効き削除は起きていない。
**MAIN と Frontend Dev の内容差は解消した。**

詳細は `ops/theme/_live_deploy_20260827_sale_price.md`。

### 決めたこと1: pull の前に必ず対象ファイルを退避する

`shopify theme pull -e live` は **修正済みのローカルファイルを MAIN 版で上書きする。**
退避していなければ D-150 の修正は消えていた。
また **ローカルにしか無いファイルも消す**(今回 `templates/collection.new-arrivals.json`)。

**手順を「退避 → pull → 復元 → md5 突き合わせ」に固定する。**
復元前に、MAIN 版との diff が**出したい変更だけ**であることを必ず確認する
(今回は +101 / -7 がすべて SUMMER SALE の表示ロジックで、MAIN 固有の変更は無かった)。

### 決めたこと2: repo は本番と同期した状態を保つ

`shopify theme pull -e live` を実行し、相違24 / repo に無い11 を**解消した**。
repo が本番と一致していれば、次に事故る余地がその分減る。
**`--only` と `--nodelete` は引き続き必ず付ける**(同期は保険を外す理由にならない)。

### 決めたこと3: ストアフロントの実測は HTTP ステータスを見る

**PDP の GET でも 503 が返る。** 28件を 400ms 間隔で回したら5件が 503 になった。
`/cart/add.js` の 429 とは別の枠。

**503 は「非セール」ではなく「取得できなかった」。**
ステータスを見ずに本文だけで判定すると、**取りこぼしを PASS と誤報告する**
(実際に最初の集計で 3件を誤って PASS にした)。
3秒からの指数バックオフで再取得し、全件 200 を取ってから集計する。

### 落とし穴: セール時の class は置き換えではなく追記

`class="moru-product__price moru-product__price--sale"` になる。
`class="moru-product__price--sale"` で引っかけようとすると**全件ハズレる**。

---

## D-153 商品ページの「特徴」をタイトル→説明→画像の順にした(2026-08-27)

**Owner 指示。** それまでは「画像 → 01 → タイトル → 説明」で、画像が先に来ていた。
**「タイトルと説明があって画像」を全ブロックで繰り返す**形に変えた。

### やり方: CSS の order ではなく DOM を並べ替えた

`sections/moru-product-features.liquid` の `__body` を `__media` より前に出した。
CSS の `order` で見た目だけ入れ替えるとスクリーンリーダーの読み上げ順が画像のままズレるため。
metafield 分岐(`product.metafields.custom.features`)と block 分岐の**両方**を直した。
実際に描画されるのは metafield 分岐(`templates/product.json` に features の block が無い)。

### 🔴 落とし穴: `assets/moru-frontend-polish.css` がセクションCSSを `!important` で上書きしている

**「PC は2カラムで左右交互(ジグザグ)」は事実ではない。** Owner の指摘で判明した。

polish.css が全ビューポートを1カラムに固定している:

```
.moru-features__row, .moru-features__row--text-only {
  grid-template-columns: minmax(0, 1fr) !important;
}
.moru-features__row--flip .moru-features__media { order: 0 !important; }
```

説明画像(日本語化した寸法図・仕様表)を切らないための意図的な設計なので**戻さない**。

このため、セクション側で `order` を触ると**壊れる**:

| 行 | セクションの `order:-1` | polish の `order:0 !important` | 結果 |
|---|---|---|---|
| 非 flip 行(01/03/05) | 効く | 無い | **画像が先に戻る** ❌ |
| flip 行(02/04) | 負ける | 効く | テキストが先 ✅ |

→ **奇数行だけ画像が先**という食い違いになる。一度この状態で本番に出してしまった。
セクション側は `order:0`(並べ替えない)に統一し、**並び順は DOM だけで決める**ことにした。

**教訓: `.moru-features__*` を触るときは必ず `assets/moru-frontend-polish.css` を先に見る。**
セクションの `{% stylesheet %}` は `compiled_assets/styles.css` に、
polish.css は独立した asset に出るので、**両方を突き合わせないと実際の効き方が分からない。**

### 検証(https://moruliving.com/products/moru-flower-lounge)

- DOM 順: 全5行(flip / 非 flip とも)`body → media` ✅
- 配信 CSS: polish.css = 1カラム固定 / styles.css = 全行 `order:0` → **DOM 順に一致** ✅
- `theme check` 100 files / 0 offenses、Dev と MAIN の両方へ反映

### 検証時の注意

ストアフロントの `<link href>` は **プロトコル相対(`//moruliving.com/...`)**。
`'https://moruliving.com' + href` で組み立てると URL が壊れて全件取得失敗し、
**「CSS に宣言が無い」と誤読する**(実際に一度踏んだ)。`//` なら `https:` を前置する。

---

## D-154 商品ページの「特徴」を記事(ブログ)組みにした(2026-08-27)

**D-153 の並び順を上書きする。** Owner から kocol の商品ページのスクリーンショットで
参考形が示された。**D-153 の「タイトル → 説明 → 画像」は短命で、これが最終。**

### 並び順: 見出し → 画像 → 説明文

D-153 では「タイトルと説明があって画像」と受け取って
**タイトル → 説明 → 画像**にしたが、参考画像は

1. 見出し(左に見出しマーク)
2. その見出しに対応する画像
3. その画像についての説明文

の順だった。**画像が見出しと説明文の間に入る**のが要点。
ひとつの見出しに画像と本文がぶら下がる、記事の一節の形になる。

### 構造

`__body`(index + title + text をまとめて持っていた)を**3つに割った**:

| ブロック | 中身 |
|---|---|
| `.moru-features__head` | `01` の連番 + `<h3>` 見出し。**左の縦バーが見出しマーク** |
| `.moru-features__media` | 画像 |
| `.moru-features__body` | 説明文 |

見出しマークは `border-inline-start: 3px solid var(--color-accent-terracotta)`。
`__head` と `__body` はどちらも `width: min(100%, 54rem)` で**左端をそろえる**
(`__body` の幅指定は polish.css 側にある)。

見出しは `<h3>` のまま。セクション見出し「この商品の特徴」が `<h2>`、商品名が `<h1>` なので、
**`<h2>` に上げると見出し階層が壊れる**(Owner の「H1やH2」は「見出し」の意)。

### ベース CSS を1カラムにそろえた

D-153 時点ではセクション側が `grid-template-columns:1fr 1fr`(2カラム)のままで、
**polish.css の `!important` に1カラム化を依存していた。**
3ブロック構成では polish を外した瞬間に崩れるので、セクション側も
`minmax(0,1fr)` にした。**これで polish.css の有無にかかわらず同じ並びになる。**

画像の `aspect-ratio:4/3` + `object-fit:cover` も外した(polish.css と同じく**切らない**方針)。

### 検証(https://moruliving.com/products/moru-flower-lounge)

- 全5ブロックで DOM 順 `見出し → 画像 → 本文` ✅ / 不一致 0
- 配信 CSS: polish.css・styles.css とも1カラムで一致。`__head` に
  `border-inline-start:3px solid var(--color-accent-terracotta)` が出ている ✅
- `theme check` 100 files / 0 offenses、Dev と MAIN の両方へ反映

### 連番は消した(2026-08-27 Owner 判断)

**`01`〜`05` の連番は削除。** 参考の kocol に連番が無く、Owner が「消してほしい」と判断した。
マークアップ(metafield 分岐・block 分岐の両方)と `.moru-features__index` の CSS を撤去。
他ファイルからの参照は無いことを確認ずみ。

`__head` の中身は `<h3>` だけになったが、**見出しマーク(左の縦バー)を持たせる器として残す。**
見出しが空のときは `__head` ごと出力しない。
